# EcoFlow Pulse — Locked Architecture & Execution Plan

**Status:** ✅ *LOCKED for development start* (Feb 2026)

EcoFlow Pulse is a resilient, multi-tier real-time monitor for streaming IoT telemetry that runs on **Web + iOS + Android** (Expo universal app) and scales from early adoption to large-scale fleets. This document is the **single source of truth** for the initial build.

---

## 1) Product scope (v1)

### Core user flows
- Social login: **Google + Facebook**
- User profile & preferences
- Device list + device detail views
- Realtime dashboard: summary stats + info cards; drill-down pages
- History:
  - today, last hour, last **3/6/12/24h**, last **7d**
  - this month, last month, this year
  - **compare vs previous period**
- Upsell modules driven by data + ML inference (online suggestions now; offline later)
- Push notifications later (device alerts / upsells) — **not day-1 critical**

### Scale & telemetry assumptions (locked)
- **12-month expected devices:** ~**1,000**
- Long-term: **10k+ devices** single region; multi-region later
- Per device telemetry: **2 msg/sec**, p95 payload **375 bytes**
- Plan for **~100 metrics** per payload (max seen ~61)
- Realtime freshness target: **250ms**, allowed to degrade under load
- Tolerable drops: **0.01%** (fits 99.99% availability)
- Availability target: **99.99% uptime** for the full critical product path:
  - authenticated access
  - device visibility
  - energy/history reads
  - realtime session continuity
  - ingest/transform/archive processing continuity
- Hot-cache durability for that target assumes Valkey is deployed with PVC-backed persistence and
  Sentinel-managed failover; ephemeral in-memory-only Valkey is not acceptable for the default
  99.99% platform recovery baseline.
- Deployment reliability is part of that target for both public traffic and background processing:
  - routine rollouts must not interrupt signed-in users,
  - ingest/transform/archive/rebuild workers must drain gracefully and avoid avoidable gaps or duplicate side effects during deploys and restarts
  - background worker rollout drain now follows ADR-0025 (`/drainz` preStop + readiness-first handoff)

---

## 2) What exists today (preserve)

### Monorepo
- Polyglot monorepo: **Go services + Expo universal client**

### Backend & runtime (Go)
- EcoFlow API client + signing: `pkg/ecoflow`
- MQTT ingestion/subscription: `pkg/ecoflowmqtt`
- HTTP server: `pkg/ecoflowserver`, `cmd/ecoflow-server`
- Distributed ingest runtime: `cmd/ecoflow-ingest-worker`
- Operational patterns: reconnect/backoff, queueing, graceful shutdown, multi-instance safety
- Compression libs: `klauspost/compress`, `andybalholm/brotli`

### Data, telemetry & ML
- Raw logs + normalized training streams
- Aggregation inputs
- CLIs for ETA/panel models, PV fingerprint, panel DB import/backfill
- Panel dataset artifacts (`*.json|*.csv`)

### Universal app (Web/iOS/Android)
- Expo + Expo Router, React Native + react-native-web
- Tamagui
- TanStack Query, Zustand, Zod
- Telemetry engine with ring buffers + snapshot clock
- Virtualization on web, charting via `@shopify/react-native-skia`
- Media assets strategy

### CI/CD
- GitHub Actions (Go tests, frontend CI, proto CI, CodeQL, db-migrations-ci, issue auto-summary)
- Makefile task orchestration

---

## 3) Locked architecture (open-source-first, swap-friendly)

### Tiering (kept)
**Expo Client (Web/iOS/Android)**  
→ **Node REST BFF (public)**  
→ **Go gRPC Data/API layer (internal)**  
→ **Data plane** (ingest/stream/projections/storage)  
→ **WebSockets Gateway (public realtime)**

### Internal gRPC baseline (ADR-0013)
All internal Go gRPC services follow a shared high-throughput baseline:
- keepalive + enforcement policy (ping-flood resistant),
- HTTP/2 transport tuning (flow-control windows, stream concurrency),
- explicit message/header limits,
- required interceptor chain:
  - request-id propagation,
  - recovery,
  - auth hook,
  - structured logging,
- reflection only in local/dev,
- graceful drain on `SIGTERM`.
- mandatory regression tests for:
  - `internal/grpcserver`,
  - `internal/grpcmw`,
  - service bootstrap packages under `cmd/` (for example `cmd/ecoflow-grpc-api` telemetry behavior).
- lock/allocation discipline on hot paths:
  - avoid per-request mutex contention unless a profiler proves it is required,
  - prefer lock-free atomics for simple monotonic counters/IDs,
  - keep immutable shared defaults for response templates to reduce alloc churn.
- goroutine/channel discipline:
  - prefer bounded channel fanout for streaming update paths,
  - define explicit drop/backpressure behavior for slow consumers,
  - avoid unbounded goroutine creation under burst loads.
- performance validation gates:
  - benchmark profiles must be derived from observed telemetry (`logs/mqtt_payload_raw-*.log`),
  - include steady-state and startup-burst scenarios,
  - keep a 10k-device synthetic soak gate with p99 latency + heap-growth thresholds.

Bootstrap packages/paths:
- `internal/grpcserver` (standardized server builder + lifecycle),
- `internal/grpcmw` (middleware scaffolding),
- `cmd/ecoflow-grpc-api` (runnable bootstrap service),
- `proto/pulse/telemetry/v1` + generated code in `gen/pulse/telemetry/v1`,
- `buf.yaml` + `buf.gen.yaml` for reproducible protobuf generation.

### Provider integration + distributed ingest control plane (ADR-0014)
The control plane uses provider-aware entities while keeping ownership primitives (`users`, `devices`, `user_devices`) stable:
- `provider_credentials`: multi-entry per user/provider, write-only secret semantics for user-facing APIs, `is_active` lifecycle control, and a DB-enforced single-active row per user/provider.
- `provider_devices`: provider-specific identity + capability metadata, credential linkage, and ingest desired state (`active | draining | paused`).
- Distributed ingest workers use Valkey lease locks (`provider + provider_device_id`) with heartbeat TTL and graceful drain behavior.
- EcoFlow discovery is manual trigger in v1; MQTT certification is fetched on connect/reconnect.

### Technology choices (locked)
- **Cloud / K8s:** **GKE** (region **us-east1**) first, portable to EKS later
- **Streaming / queue:** **NATS JetStream**
- **Live cache runtime:** **Valkey** (Redis-compatible) in **replication + Sentinel** mode
- **Shared cache substrate:** canonical keys, envelopes, compression, encryption,
  tag invalidation, and cache metrics as defined in ADR-0028
- **Control-plane DB:** **Postgres** (CloudNativePG operator)
- **Telemetry rollups/history v1:** **TimescaleDB** (Postgres extension)
- **Raw archive + replay source of truth:** object storage, **protobuf + zstd**
  - local: **MinIO**
  - prod: **GCS**
- **Auth:** **Keycloak** (OIDC) with Google + Facebook
- **S2S security:** mTLS via **Linkerd** (recommended; can be enabled after M1 if desired)
- **GitOps:** **Argo CD**
- **Browser edge:** TLS at ingress, browser-facing **HTTP/2** by default, and
  **HTTP/3** when QUIC + UDP `443` are explicitly exposed at the ingress path
  (ADR-0017)
- **Observability:** OpenTelemetry Collector + Prometheus/Grafana; Loki/Tempo “lite” initially

### SLO measurement rule (locked)
- For user-facing REST paths, the primary SLI source must be client-observed request outcomes from first-party clients, not server-only request logs.
- Server-side HTTP metrics remain required for debugging and correlation, but they are supporting signals when the question is user-perceived availability or latency.
- Client-observed REST SLOs must use canonical low-cardinality route labels and request-based math:
  - availability = good requests / total requests
  - latency = at least `P95` and `P99` client-observed duration for successful requests
  - errors = explicit client-observed failures, including network/transport failures that may never reach the server
- For user-facing websocket/realtime paths, the primary SLI source must be client-observed websocket lifecycle and freshness events from first-party clients, not gateway-only handshake/process metrics.
- Server-side websocket metrics remain required for debugging and capacity analysis, but they are supporting signals when the question is user-perceived realtime availability, reconnect behavior, or stale live data.
- Client-observed websocket SLOs must use canonical low-cardinality labels and client-visible math:
  - availability = successful usable connects/reconnects / total client-observed connect outcomes
  - latency = at least `P95` and `P99` client-observed connect and reconnect duration
  - errors = explicit auth-required/auth-failed outcomes plus unexpected disconnect reasons
  - freshness = client-observed stale transitions and stale-recovery duration as supporting context

### Why Valkey
- Open-source posture and compatibility with Redis OSS clients.
- Keeps cost/ops simple for v1 with a clean swap path to hosted caches later.
- Cache behavior and Valkey runtime are intentionally split in ADR-0028 and
  ADR-0029 so future backend changes do not rewrite service-level cache policy.

---

## 4) Data retention & replay (locked)

### Retention (locked)
- **Raw telemetry archive:** **30 days** (protobuf+zstd objects)
- **Minute rollups:** **90 days**
- **Hourly rollups:** **3 years**
- **Daily rollups:** **3 years**

### Replay (hard requirement)
MQTT is not queryable for historicals, so replay must be first-class.

Replay sources:
- **Authoritative:** raw archive objects (30d)
- **Operational:** JetStream retention (24–72h) for fast incident recovery

Replay modes:
- Per-device replay (single device or list)
- Fleet/system replay (by shard/time range)
- Gap repair (devices with holes in last 24h)

---

## 5) Realtime delivery (WebSockets)

A dedicated **WS Gateway** service:
- Authenticates WS (JWT)
- Authorizes per-device subscriptions (owner/guest RBAC)
- On subscribe:
  - sends snapshot from Valkey
  - streams deltas from NATS
- Enforces per-connection backpressure + downsampling:
  - **250ms → 500ms → 1s → key-metrics-only → paused**

This ensures stability without “circuit breakers”.

---

## 6) AuthN/AuthZ (authoritative requests)

- Client auth: Keycloak **Authorization Code + PKCE**
- Node REST BFF validates JWT via JWKS
- Node → Go gRPC forwards **user JWT** in metadata
- Go gRPC validates JWT again and enforces authz via:
  - `user_devices` mapping (`viewer | admin`)

This ensures:
- REST cannot be forged to access other users’ devices
- gRPC does not “trust” Node implicitly

### CI merge gates (required)
The `main` branch is protected by required CI checks. These check names are part of the architecture contract and must remain stable:

- `go-test`
- `go-test-race-critical`
- `frontend-ci`
- `proto-ci`
- `CodeQL`
- `db-migrations-ci`

If a workflow or check name changes, update repository branch protection/rulesets in the same change so merges are never silently unblocked.

---

## 7) Local dev principle (keep it very simple)

**Local runs on Kubernetes too** and mirrors dev behavior:
- local cluster: **k3d**
- one command to bring up platform deps + core services
- services default to in-cluster execution (parity > cleverness)

Suggested developer commands (you’ll implement in Makefile):
- `make k3d-up` — create local cluster
- `make platform-up` — install platform umbrella chart (NATS/CNPG/Valkey/Keycloak/MinIO)
- `make services-up` — install services umbrella chart
- `make dev-up` — does all three
- `make dev-down` — uninstall + optionally delete cluster

Optional (not required): Tilt for hot-reload convenience.

### When to use GKE dev (cost-min policy)
Use GKE dev only for cloud-only validation:

1. OAuth/social redirect flows on real domains/devices
2. ingress + TLS + cert-manager behavior
3. Workload Identity and External Secrets behavior
4. GCS lifecycle/retention checks for archive flows
5. autoscaling/node lifecycle behavior, Argo CD cloud sync realism, and
   future node/storage-shape validation:
   - general/public pool on `e2-standard-2`,
   - CNPG anchor on `e2-standard-4`,
   - SSD-backed PVCs where stateful latency and durability matter

Everything else should run on local k3d first.  
When not actively testing in GKE dev, park workloads and reduce node-pool mins.

---

# 8) Milestones & status board

Legend: **TODO | PROGRESS | DONE | HELP**

## Decisions log (DONE)
- **DONE:** Cloud = **GKE**
- **DONE:** Region = **us-east1**
- **DONE:** Streaming bus = **NATS JetStream**
- **DONE:** Cache = **Valkey** (replication + Sentinel)
- **DONE:** DB = Postgres + TimescaleDB (v1)
- **DONE:** Archive = **protobuf + zstd**
- **DONE:** Raw retention = **30 days**
- **DONE:** Minute rollups retention = **90 days**
- **DONE:** Hourly/daily rollups retention = **3 years**
- **DONE:** Realtime = **WebSockets**
- **DONE:** Auth = **Keycloak** w/ Google & Facebook
- **DONE:** CI merge gates = **go-test + go-test-race-critical + frontend-ci + proto-ci + CodeQL + db-migrations-ci**

---

## M0 — Platform baseline (GKE + local k3d parity)
**Goal:** “Small HA feel” everywhere; costs low; local dev is simple.

| Status | Task | Dependency |
|---|---|---|
| DONE | Create `/deploy` layout (charts + env values + Argo apps)<br>- [x] Implemented `/deploy` scaffold (local+dev only, namespaces agreed)<br>- [x] Added direct Argo apps (`pulse-platform`, `pulse-services`)<br>- [x] Added local k3d config at `deploy/tilt/k3d-config.yaml`<br>- [x] Wired initial platform chart dependencies (scaffold-first, disabled by default)<br>- [x] Pinned initial dependency chart versions (`nats`, `cloudnative-pg`, `valkey`, `keycloak`, `minio`)<br>- [x] Added `Chart.lock` for reproducible Helm dependency resolution | — |
| DONE | Local k3d cluster config + Make targets (`dev-up`)<br>- [x] Added k3d cluster config (`deploy/tilt/k3d-config.yaml`)<br>- [x] Implemented Make targets (`k3d-up`, `platform-up`, `platform-wait`, `services-up`, `services-wait`, `dev-up`, `dev-down`)<br>- [x] Hardened `platform-up` with retry/backoff for transient CNPG webhook race conditions<br>- [x] Enforced startup ordering in `dev-up` (`platform-wait` before `services-up`) so developers do not need double-runs<br>- [x] Documented local bringup/down usage and defaults | — |
| DONE | Argo CD bootstrapped in GKE dev<br>- [x] Add GKE Argo CD bootstrap Make targets (`argocd-bootstrap-dev`, app apply/sync validation)<br>- [x] Add Argo CD dev values profile with cost-min defaults<br>- [x] Document bootstrap + validation commands for developers<br>- [x] Validate end-to-end against real GKE dev cluster and capture output (`ecoflow-pulse-dev-260221-01`, `make argocd-dev-up`, apps reached `Synced + Healthy`) | cluster |
| DONE | ingress-nginx + cert-manager<br>- [x] Add and pin Helm dependencies (`ingress-nginx`, `cert-manager`) in `pulse-platform` umbrella chart<br>- [x] Add local/dev values scaffolding and component toggles (`components.ingressNginx`, `components.certManager`)<br>- [x] Extend `platform-wait` readiness checks for ingress + cert-manager controllers<br>- [x] Validate local render/install path (`helm dependency update`, `helm lint` with local/dev values)<br>- [x] Validate GKE dev Argo sync with cert-manager CRDs and ingress controller ready (temporary branch-target app patch + hard refresh, then restore `targetRevision=main`) | cluster |
| DONE | External Secrets Operator (staging/prod)<br>- [x] Added and pinned `external-secrets` chart dependency in `deploy/charts/pulse-platform/Chart.yaml`<br>- [x] Added component toggle scaffolding (`components.externalSecrets`) and base values in `deploy/charts/pulse-platform/values.yaml`<br>- [x] Added local/dev values wiring (`deploy/env/local/values.platform.yaml`, `deploy/env/dev/values.platform.yaml`)<br>- [x] Extended `make platform-wait` readiness checks for ESO controller/webhook/cert-controller deployments<br>- [x] Validated local render path (`helm dependency update`, `helm lint` local/dev values)<br>- [x] Validated GKE dev Argo sync + readiness and recorded output (`pulse-platform`/`pulse-services`: `Synced+Healthy`; ESO controller/webhook/cert-controller `1/1` Ready) | cluster |
| DONE | CloudNativePG operator + base Postgres<br>- [x] Enabled CloudNativePG operator in `deploy/env/local/values.platform.yaml` (`components.cloudnativepg.enabled=true`)<br>- [x] Added base CNPG Postgres cluster manifests to `pulse-platform` chart (`cloudnativepgCluster` values + `Cluster` CR + app credentials secret template)<br>- [x] Configured local base Postgres HA footprint (`instances=2`, `storage=10Gi`, resource requests/limits per Config-03 defaults)<br>- [x] Added CRD-safe reconcile workflow in `make platform-up` (wait for CNPG operator, then second Helm pass for CRD-backed resources)<br>- [x] Validated operator + cluster readiness in local k3d (`make platform-up`, `kubectl get clusters.postgresql.cnpg.io -n pulse-platform`, `kubectl get pods -n pulse-platform`)<br>- [x] Wired service-facing connection contract for `pulse-platform-core-rw` (`pulse-platform-core-contract` ConfigMap + `pulse-platform-core-connection` Secret)<br>- [x] Switched Keycloak from bundled Postgres to CNPG base Postgres (`keycloak.postgresql.enabled=false`, `keycloak.externalDatabase.*` to `pulse-platform-core-rw`) | cluster |
| DONE | TimescaleDB enablement (extension)<br>- [x] Added TimescaleDB configuration to CNPG cluster values (`cloudnativepgCluster.timescaledb.*`)<br>- [x] Enabled local TimescaleDB component wiring (`components.timescaledb.enabled=true`)<br>- [x] Added CNPG `ImageCatalog` + `imageCatalogRef` support for Timescale image selection (major `18`), plus `postInitApplicationSQL` extension bootstrap in app DB<br>- [x] Added Timescale-specific runtime UID/GID wiring and `postgresql.shared_preload_libraries: [timescaledb]` in cluster template<br>- [x] Validated cluster reconciliation after image switch (`make platform-up`, `make platform-wait`)<br>- [x] Validated extension installation and preload on primary (`SHOW shared_preload_libraries;`, `SELECT extname FROM pg_extension`) | Postgres |
| DONE | NATS JetStream (3 replicas)<br>- [x] Added + pinned NATS dependency in `pulse-platform` chart<br>- [x] Enabled NATS JetStream (3 replicas) in `deploy/env/local/values.platform.yaml`<br>- [x] Kept dev NATS config scaffolded but disabled by default<br>- [x] Validated `make platform-up` end-to-end on local k3d cluster | cluster |
| DONE | Valkey (Sentinel, 3 pods total)<br>- [x] Enabled Valkey component in `deploy/env/local/values.platform.yaml`<br>- [x] Configured replication + Sentinel topology for 3 pods total in local k3d (`replica.replicaCount=3`, `sentinel.enabled=true`)<br>- [x] Validated with `make platform-up` + pod health checks (`kubectl get sts/pods -n pulse-platform`)<br>- [x] Documented local validation commands/results | cluster |
| DONE | Keycloak (2 replicas)<br>- [x] Enabled Keycloak component in `deploy/env/local/values.platform.yaml`<br>- [x] Configured local Keycloak with 2 replicas using external CNPG Postgres (`pulse-platform-core-rw` via `keycloak.externalDatabase.*`)<br>- [x] Added local image repository overrides to `bitnamilegacy/*` to avoid pull failures during local bringup<br>- [x] Set explicit Keycloak container resources (`requests: 250m/1Gi`, `limits: 1 CPU/2Gi`) to prevent OOM restarts in k3d<br>- [x] Validated with `make platform-up` + `kubectl get pods -n pulse-platform` (both `pulse-platform-keycloak-0` and `pulse-platform-keycloak-1` Ready)<br>- [x] Documented node recovery step after Docker restarts (`docker restart k3d-pulse-local-agent-0`) | cluster |
| DONE | MinIO (local raw archive)<br>- [x] Enabled MinIO component in `deploy/env/local/values.platform.yaml`<br>- [x] Configured local MinIO for standalone + PVC-backed durable mode (`mode=standalone`, `replicas=1`, `persistence.enabled=true`)<br>- [x] Added keep-style PVC annotation for local raw archive retention across routine Helm lifecycle (`helm.sh/resource-policy=keep`)<br>- [x] Tuned local resource requests to schedule in k3d (`memory=512Mi`, `cpu=100m`)<br>- [x] Documented that local MinIO is authoritative for replay/rebuild trust and must not be ephemeral | local |
| DONE | Observability “lite” installed<br>- [x] Added and pinned `kube-prometheus-stack` and `opentelemetry-collector` chart dependencies in `deploy/charts/pulse-platform/Chart.yaml`<br>- [x] Added `components.observabilityLite` toggle + low-footprint defaults in `deploy/charts/pulse-platform/values.yaml`<br>- [x] Added dev values profile with observability enabled and constrained resources (`deploy/env/dev/values.platform.yaml`)<br>- [x] Added required OTel collector image repository wiring for chart renderability (`otel/opentelemetry-collector-k8s`)<br>- [x] Extended `make platform-wait` readiness checks for Grafana, Prometheus operator, and OTel collector deployments<br>- [x] Validated local render path (`helm dependency update`, `helm lint` local/dev values)<br>- [x] Validated GKE dev Argo sync + readiness and recorded output (`pulse-platform`/`pulse-services`: `Synced+Healthy`; Grafana + Prometheus operator + OTel collector running; Prometheus StatefulSet ready after hard refresh) | cluster |
| DONE | Hosted cloud node-pool + SSD optimization<br>- [x] Mark task `PROGRESS` and define the branch-time implementation checklist<br>- [x] Audit live `pulse-cloud` node pools, PVC storage classes, and workload placement before changing scheduling<br>- [x] Update cloud placement guidance/manifests so the general/public pool can move toward `e2-standard-2` while the stateful anchor keeps a dedicated larger node shape<br>- [x] Keep stateful durability/performance on SSD-backed PVCs (`standard-rwo` / `pd-balanced`) for CNPG, NATS JetStream, and Valkey instead of sizing node boot disks around data retention<br>- [x] Revisit node boot disks after the pool split and reduce them to `50Gi` `pd-balanced` once PVC-backed durability is in place<br>- [x] Validate Argo health, final workload placement, and live cloud readiness after the pool/storage changes<br>- [x] Record the live recovery result from the `2026-04-19` zonal-capacity incident: `pulse-platform-cloud` + `pulse-services-cloud` returned to `Synced/Healthy`, with CNPG/NATS/Valkey restored on a `us-east1-c` stateful anchor without data loss | cluster |
| DONE | Hosted cloud stateful HA + multi-zone replication<br>- [x] Mark task `PROGRESS` and define the implementation checklist for this branch<br>- [x] Add a multi-zone stateful capacity plan so cloud keeps at least one additional zone available for new replicas instead of concentrating every stateful pod in `us-east1-c`<br>- [x] Raise CNPG cloud defaults above `instances=1` and document zone-aware placement/failover expectations<br>- [x] Move cloud NATS from single-node JetStream to a replicated cluster shape with zone-aware placement<br>- [x] Raise cloud Valkey replica count and Sentinel quorum so failover can survive the loss of one stateful node/zone<br>- [x] Evaluate regional PD / Hyperdisk Balanced HA where disk-level zonal resilience is worth the extra cost, and document the migration path for existing zonal PVCs<br>- [x] Validate cloud chart render/lint and record the recommended rollout path for the live cluster<br>- [x] Provision live additional stateful capacity in `us-east1-d` and `us-east1-b` (`stateful-pool-ha`, `stateful-pool-quorum`) and roll the hosted platform to CNPG `2`, NATS `3`, and Valkey `3`<br>- [x] Complete the one-time NATS/Valkey StatefulSet recreate so all live broker/replica PVCs converge onto `standard-rwo` / `20Gi`, and clear the superseded unattached disks afterward | cluster |
| DONE | Hosted cloud zonal DB HA + public path zonal HA<br>- [x] Mark task `PROGRESS` and define the implementation checklist for this branch<br>- [x] Add efficient public-path capacity in a second zone without increasing the permanent app-node type footprint; the live rollout now uses `app-pool` (`e2-standard-2`, `us-east1-d`) plus spare `stateful-pool-quorum` capacity in `us-east1-b` after `us-east1-b` stockout blocked a new `e2-standard-2` app pool<br>- [x] Spread ingress, public app, realtime gateway, and Keycloak across at least two zones for public/auth-path HA<br>- [x] Rehome one CNPG instance off `us-east1-c` so DB HA is genuinely zonal instead of two PVCs in one zone<br>- [x] Validate that the live node types remain efficient after the zone-spread changes and avoid unnecessary upscale on the public path<br>- [x] Re-verify `pulse-platform-cloud` and `pulse-services-cloud` as `Synced + Healthy` with the new zone placement evidence captured in docs | cluster |
| PROGRESS | Raspberry Pi 5 appliance migration<br>- [x] Create Ralph-loop plan/task/role-memory scaffold under `.codex/`<br>- [x] Lock the appliance decisions in `docs/architecture/config-06-pi5-appliance.md`<br>- [x] Phase 1: host install, K3s config, `deploy/env/pi/`, status tooling<br>  - [x] Add Pi host preparation/status scripts, K3s config artifact, and script fixture coverage<br>  - [x] Add Pi platform/services Helm overlays with singleton/conservative defaults<br>  - [x] Add chart support for CNPG Postgres parameters and loopback gRPC hostPort<br>  - [x] Add installer/upgrade orchestration with dry-run coverage<br>- [x] Phase 2: BLE direct gRPC, durable edge outbox, idempotent retries<br>  - [x] Add direct gRPC transport for enrollment, heartbeat, discovery, and telemetry<br>  - [x] Add durable BLE outbox with stable client sample identity and idempotent replay<br>- [x] Phase 3: GCS archive upload outbox, backup/restore, planned cutover<br>  - [x] Add SSD-backed archive upload outbox foundation with ACK-after-fsync behavior<br>  - [x] Add backup/restore and planned cutover runbook<br>  - [x] Add fail-closed rebuild/status checks for pending local archive uploads<br>- [ ] Phase 4: conservative workload consolidation and capacity burn-in<br>  - [x] Add per-workload Go runtime caps for the current singleton services layout<br>  - [x] Add capacity burn-in runbook and render validation for Pi runtime caps<br>  - [x] Add release-values, image preflight, and GCS credential secret plumbing before live burn-in<br>  - [x] Add GHCR Pi appliance image publishing workflow and digest release artifact<br>  - [ ] Run real Pi 24h burn-in with 10-device target load<br>  - [ ] Decide which workloads can safely merge after hardware evidence<br>- [ ] Phase 5: appliance acceptance, product review, and cloud-shutdown readiness<br>Implementation uses the Pulse Pi 5 appliance Ralph Loop.<br>Validation evidence (2026-06-04): `make lint` succeeded for the plan-only docs/scaffold change; `make appliance-pi-validate` succeeded for the first Phase 1 scaffold slice; `make appliance-pi-validate` succeeded again after adding installer/upgrade orchestration and dry-run coverage; `go test ./cmd/pulse-edge-collector -count=1`, `go test ./cmd/ecoflow-grpc-api ./internal/edgecollector -count=1`, `make pulse-edge-pi5-bundle` with `GOARM64=v8.2`, `make appliance-pi-validate`, and `make lint` succeeded for the BLE direct-gRPC slice and review follow-up. Durable outbox validation added `go test ./cmd/pulse-edge-collector ./cmd/ecoflow-grpc-api ./internal/edgecollector ./internal/envelopededup ./internal/rollupworker ./internal/archiveworker -count=1`. Archive upload outbox foundation validation added `go test ./internal/archiveworker ./cmd/ecoflow-archive-worker -count=1`, Pi/local services Helm lint, Pi render inspection for `ARCHIVE_UPLOAD_OUTBOX_*` and the `go-archive-outbox` PVC, `make appliance-pi-validate`, targeted archive `go test -race`, `make test-race`, `make lint`, and `git diff --check`. Status/rebuild guard validation added `go test ./internal/archiveworker ./cmd/ecoflow-archive-outbox-status ./cmd/ecoflow-rollup-rebuild -count=1`, `make appliance-pi-validate`, targeted guard `go test -race`, `make test-race`, `make lint`, and `git diff --check`. Backup/cutover runbook validation added `make lint`, `git diff --check`, and the duplicate editor-backup file scan. Runtime-cap validation added `make appliance-pi-validate`, `make lint`, `git diff --check`, duplicate editor-backup file scan, and Helm render inspection for Pi `GOMAXPROCS` / `GOMEMLIMIT` values. Release-input validation added digest image rendering, placeholder-image preflight, GCS credential mount rendering, runtime secret helper coverage, and `make appliance-pi-validate`. Image-publishing validation added release artifact rendering from workflow-compatible inputs, optional GHCR pull-secret rendering, `make appliance-pi-validate`, and `make lint`. | M0 + ADR-0006 + ADR-0014 + edge collector |

Live Pi validation note (2026-06-06): the first full appliance rollout reached
deployed `pulse-platform` and `pulse-services` Helm releases with all singleton
services running. The follow-up hardening slice validates with
`go test ./internal/dbmigrate -count=1`, `make appliance-pi-validate`,
`make lint`, `git diff --check`, and duplicate editor-backup scan.

**Acceptance criteria**
- [x] Local: `make dev-up` yields a working platform + services (`k3d-pulse-local`; local Make targets pinned to k3d context to avoid accidental GKE applies)
- [x] Dev: Argo sync yields a working platform + services (`pulse-platform` and `pulse-services` = `Synced + Healthy` in `argocd` namespace)
- [x] Failover behavior can be observed (CNPG, Valkey, NATS restarts)
  - CNPG failover validated: cluster returned to `Ready=True` with 2 healthy instances after primary restart
  - NATS validated: StatefulSet recovered to `3/3`
  - Valkey validated: StatefulSet recovered to `3/3`

---

## M1 — Identity + control plane
| Status | Task | Dependency |
|---|---|---|
| DONE | Internal Go gRPC baseline bootstrap (ADR-0013)<br>- [x] Imported shared gRPC server builder (`internal/grpcserver`) with keepalive enforcement, HTTP/2 tuning, stream/message limits, reflection-gated-by-env, and graceful SIGTERM drain<br>- [x] Imported standard middleware scaffold (`internal/grpcmw`): request-id, recovery, auth hook, structured logging<br>- [x] Added bootstrap telemetry service (`proto/pulse/telemetry/v1`) + generated stubs under `gen/pulse/telemetry/v1` via Buf<br>- [x] Added runnable server entrypoint (`cmd/ecoflow-grpc-api`) with health service and telemetry registration<br>- [x] Added regression tests for `internal/grpcserver`, `internal/grpcmw`, and `cmd/ecoflow-grpc-api` telemetry service behavior to prevent baseline regressions<br>- [x] Added workload-calibrated soak/benchmark coverage and GC/pprof profiling workflow based on `logs/mqtt_payload_raw-*.log` characteristics<br>- [x] Added 10k-device synthetic fleet soak benchmark with p99 latency and heap-growth thresholds (opt-in)<br>- [x] Replaced `NoopAuthorizer` production path with Keycloak JWKS validation mode and enforced token-subject boundary checks in `ControlPlaneService` (`user_subject` must match JWT `sub`) | M0 |
| DONE | Keycloak realm + Google/Facebook providers<br>- [x] Added chart-managed Keycloak realm import ConfigMap template (`deploy/charts/pulse-platform/templates/keycloak-realm-configmap.yaml`)<br>- [x] Added chart-managed social provider secret template (`deploy/charts/pulse-platform/templates/keycloak-social-secret.yaml`)<br>- [x] Wired local values to enable `keycloakConfigCli` with `existingConfigmap` + `extraEnvVarsSecret`<br>- [x] Added local verification target (`make auth-keycloak-verify-local`) to assert realm + provider presence via `kcadm`<br>- [x] Validate local end-to-end (`make platform-up` + `make platform-wait` + `make auth-keycloak-verify-local`)<br>- [x] Add dev secrets contract + provider credential bootstrap doc (`docs/how-to/configure-keycloak-social-providers-local.md`) | M0 |
| DONE | Expo PKCE auth flow<br>- [x] Added Keycloak OIDC PKCE session card to universal app settings (`apps/universal/src/features/auth/KeycloakPkceCard.tsx`)<br>- [x] Added persisted auth session store (`apps/universal/src/features/auth/store.ts`) with sign-in / refresh / sign-out lifecycle<br>- [x] Added OIDC runtime env wiring (`EXPO_PUBLIC_OIDC_ISSUER_URL`, `EXPO_PUBLIC_OIDC_CLIENT_ID`, `EXPO_PUBLIC_OIDC_AUDIENCE`, `EXPO_PUBLIC_OIDC_SCOPES`) | Keycloak |
| DONE | Postgres schema + migrations (`users`, `devices`, `user_devices`)<br>- [x] Added initial migration scaffold under `deploy/db/migrations` (`000001_m1_control_plane_schema.up.sql`, `.down.sql`)<br>- [x] Applied UUIDv7 IDs (`uuidv7()`) across `users` and `devices`<br>- [x] Enforced `keycloak_subject` unique+required and `ecoflow_sn` unique+global<br>- [x] Added `user_devices` composite PK (`user_id`, `device_id`) with role check (`viewer/admin`)<br>- [x] Locked app-managed UTC timestamps (`created_at`, `updated_at`) with no DB defaults<br>- [x] Added migration apply/verify command path for local k3d CNPG DB (`make db-migrate-up-local`, `make db-migrate-down-local`, `make db-migrate-verify-local`, `make db-migrate-cycle-local`, `make db-migrate-e2e-local`)<br>- [x] Validated migration end-to-end against local platform DB (`make db-migrate-cycle-local` + `make db-migrate-e2e-local`; verified `uuidv7()` ID default, check constraints, uniqueness constraints, ownership join path, and app-managed timestamp columns) | M0 |
| DONE | Provider integrations + device metadata control-plane model (ADR-0014)<br>- [x] Locked architecture decision for provider-scoped credentials + provider device identity (`provider_credentials`, `provider_devices`)<br>- [x] Locked write-only secret policy for user-facing credential APIs<br>- [x] Locked manual discovery-only behavior for v1 (`DiscoverDevices()` explicit trigger)<br>- [x] Locked lease protocol choice (Valkey TTL + heartbeat + graceful drain)<br>- [x] Added migration files for `provider_credentials` + `provider_devices` (`000002_m1_provider_integrations_schema`) with UUIDv7 + UTC app-managed timestamps<br>- [x] Validate provider integration migration cycle/e2e on local CNPG (`make db-migrate-cycle-local` + `make db-migrate-e2e-local`)<br>- [x] Add control-plane APIs: credentials CRUD/list, `DiscoverDevices()`, provider-grouped `ListDevices()`<br>  - [x] Define protobuf contract for `ControlPlaneService` and generate stubs<br>  - [x] Implement service scaffolding + store abstraction in `cmd/ecoflow-grpc-api`<br>  - [x] Implement credential CRUD/list path (write-only secret semantics)<br>  - [x] Implement provider-grouped `ListDevices()` using ownership joins<br>  - [x] Add manual `DiscoverDevices()` trigger stub with provider hook contract<br>- [x] Add explicit dev seed command using `ECOFLOW_DEV_ACCESS_KEY` / `ECOFLOW_DEV_SECRET_KEY` and initial SN bindings (`cmd/ecoflow-dev-seed` + `make db-seed-dev-local`)<br>Validation evidence (2026-03-21): the `/devices` product flow now keeps configured devices in the main list while exposing a separate manual “Available devices” section below it; that section stays lazy until the user explicitly scans, never reuses cached available-device results between scans, lists unconfigured provider devices without persisting them, skips unusable active credentials whose provider material cannot support discovery, uses the `Enable and Activate` action to run a successful live MQTT probe and atomically persist activation server-side, uses probe-only MQTT client IDs so diagnostics cannot displace ingest sessions, and then activates the selected `provider_device` with `ingest_desired_state=active` so the ingest worker can claim it without relying on quota support. Targeted validation succeeded with `go test ./cmd/ecoflow-grpc-api/... ./internal/provideradapter ./internal/ingestworker -count=1`, `npm run -w apps/pulse-platform test -- test/device_routes.test.ts test/device_client.test.ts test/history_routes.test.ts test/me_routes.test.ts test/weather_routes.test.ts`, and `npm run -w apps/pulse-platform typecheck`. | M1 schema + ADR-0014 |
| DONE | Node JWT middleware (JWKS)<br>- [x] Added reusable Node package `@ecoflow-pulse/node-jwks-auth` with JWKS verifier and Express-style middleware contract (`packages/node-jwks-auth/src/index.ts`)<br>- [x] Added unit tests for bearer parsing, middleware behavior, and live JWKS verification (`packages/node-jwks-auth/test/jwks_auth.test.ts`)<br>- [x] Added frontend CI gates for package typecheck + tests (`.github/workflows/frontend-ci.yml`) | Keycloak |
| DONE | Go gRPC JWT interceptor + authz<br>- [x] Added auth interceptors (`internal/grpcmw/auth.go`) and Keycloak JWKS authorizer (`internal/grpcmw/keycloak_authorizer.go`)<br>- [x] Added `GRPC_AUTH_MODE=keycloak` runtime path in `cmd/ecoflow-grpc-api/main.go` with issuer/audience validation<br>- [x] Enforced token-subject boundary in control-plane service (`resolveUserSubject`) | Keycloak |
| DONE | Device registry APIs (create/link/list)<br>- [x] Added protobuf API contract (`CreateDevice`, `LinkDevice`, `ListUserDevices`) in `proto/pulse/controlplane/v1/control_plane.proto` + generated stubs<br>- [x] Implemented store methods in memory + Postgres control-plane stores (`internal/controlplane/store_memory.go`, `internal/controlplane/store_postgres.go`)<br>- [x] Implemented gRPC service handlers in `cmd/ecoflow-grpc-api/controlplane_service.go` | schema + auth |
| DONE | RBAC (viewer/admin) end-to-end<br>- [x] Enforced admin-only sharing on `LinkDevice` and role validation (`viewer`, `admin`)<br>- [x] Added end-to-end service/store tests for create/list/link + RBAC denial path (`cmd/ecoflow-grpc-api/controlplane_service_test.go`, `internal/controlplane/store_memory_test.go`) | schema + auth |
| DONE | Authenticated entry routing, profile preferences, and device authorization (ADR-0020)<br>- [x] Import ADR-0020 into the repo and lock the Google-visible / Facebook-hidden increment scope<br>- [x] Bootstrap Ralph-loop plan/task/memory tracking under `.codex/` for this project<br>- [x] Extend the control-plane schema and store for current-user profile preferences + first-sign-in provisioning<br>- [x] Add Go current-user bootstrap/read/update RPCs<br>- [x] Tighten unauthorized device/realtime authorization semantics<br>- [x] Add Node BFF `GET /api/v1/me` and `PATCH /api/v1/me` endpoints with strict validation and gRPC error mapping<br>- [x] Add universal welcome page, login page, protected-route redirect-back, and logout flow<br>- [x] Add standalone `/profile` page with display name, readonly email, readonly avatar/sign-in method, IANA timezone typeahead selection, and weather-location consent/revoke behavior<br>- [x] Add background social-profile/avatar backfill through `/api/v1/me/identity-refresh` when provider avatar data is still missing at profile render time<br>- [x] Emit auth/profile OTEL-compatible metrics and add Grafana dashboards for login, redirect, provisioning, unauthorized access, profile updates, and websocket auth behavior<br>- [x] Add QA coverage for redirect-back, unauthorized access, logout teardown, and profile updates<br>- [x] Replace manual token refresh with automatic session renewal before access-token expiry so signed-in sessions stay alive during normal use<br>- [x] Harden rolling deploy behavior so signed-in users do not see transient bootstrap/auth/onboarding errors during pod rollouts<br>  - [x] Set public-facing Deployments to RollingUpdate with `maxUnavailable: 0` and non-zero `maxSurge`<br>  - [x] Add graceful termination windows and `preStop` drain hooks for public app + realtime gateway<br>  - [x] Add Pod Disruption Budgets for auth/bootstrap/realtime public workloads<br>  - [x] Revalidate readiness probes so traffic only reaches actually ready pods during rollout<br>  - [x] Preserve prior homepage/profile/history data during routine refetch so rollouts do not flash empty-state content<br>- [x] Harden cold-start recovery so a full local platform restart converges back to healthy without manual intervention<br>  - [x] Add startup/readiness behavior that tolerates normal post-restart warmup without false crash loops<br>  - [x] Add bounded startup retry/backoff to ingest/transform/archive workers for Postgres/NATS/Valkey/object-store dependencies<br>  - [x] Strengthen dependency/recovery gating so local bringup waits for genuinely healthy platform services<br>- [x] Add graceful request/websocket drain expectations and acceptance evidence for seamless deploys<br>- [x] Validate local k3d flow and capture acceptance evidence before marking `DONE`<br>Validation evidence (2026-03-13): `npm run -w apps/pulse-platform test -- device_routes.test.ts history_routes.test.ts me_routes.test.ts` succeeded; `npm run -w apps/pulse-platform typecheck` succeeded; `npm run -w apps/pulse-realtime-gateway test -- gateway.test.ts` succeeded; `npm run -w apps/pulse-realtime-gateway typecheck` succeeded; `npm run -w apps/universal test -- src/features/auth/useReturnTo.test.ts src/features/auth/useRequireAuth.test.ts src/features/auth/LogoutButton.test.ts src/features/profile/hooks.test.ts src/features/telemetry/store.test.ts src/shared/config/env.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `helm lint deploy/charts/pulse-platform -f deploy/env/local/values.platform.yaml` and `helm lint deploy/charts/pulse-platform -f deploy/env/dev/values.platform.yaml` succeeded after wiring route-level auth guards, the Pulse-branded `/login` sign-in entry, post-login redirect resolution, templated `/onboarding`, logout telemetry teardown, authenticated `403` vs unauthenticated `401` device/realtime semantics, frontend auth-guard/logout/profile-cache regressions, public app `/metrics`, realtime gateway `/metrics`, ServiceMonitor wiring, and the Pulse Auth & Profile Grafana dashboard. Live local edge checks also confirmed `curl -k -I https://localhost/login` returned `HTTP/2 200` and `curl -k https://localhost/realms/pulse/.well-known/openid-configuration` returned the `https://localhost/realms/pulse` issuer with HTTPS auth/token endpoints.<br>Validation evidence (2026-03-14): `npm run -w apps/universal test -- src/features/auth/store.test.ts src/features/auth/sessionRefresh.test.ts src/features/auth/useRequireAuth.test.ts src/features/auth/LogoutButton.test.ts` succeeded and `npm run -w apps/universal typecheck` succeeded after adding automatic OIDC session refresh and expired-session recovery gating in the shared app provider.<br>Validation evidence (2026-03-14): `helm lint deploy/charts/pulse-platform -f deploy/env/local/values.platform.yaml`, `helm lint deploy/charts/pulse-platform -f deploy/env/dev/values.platform.yaml`, `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml`, and `helm lint deploy/charts/pulse-services -f deploy/env/dev/values.services.yaml` succeeded after adding zero-downtime RollingUpdate strategy, startup probes, termination grace, and PodDisruptionBudgets for public auth/bootstrap/realtime workloads and the Go API deployments behind them.<br>Validation evidence (2026-03-14): `npm run -w apps/pulse-platform test -- me_routes.test.ts` succeeded; `npm run -w apps/universal test -- src/features/profile/hooks.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `go test ./internal/startupretry ./cmd/ecoflow-ingest-worker ./cmd/ecoflow-projection-worker ./cmd/ecoflow-rollup-worker ./cmd/ecoflow-archive-worker ./cmd/ecoflow-inference-worker ./cmd/ecoflow-grpc-api -count=1` succeeded; `helm lint deploy/charts/pulse-platform -f deploy/env/local/values.platform.yaml` succeeded after adding background identity/avatar refresh, stale-while-refresh query retention on homepage/profile/history views, and bounded worker startup retry for core data-plane dependencies.<br>Validation evidence (2026-03-14): `npm run -w apps/pulse-realtime-gateway test -- gateway.test.ts` includes shutdown coverage proving websocket sessions close and live subscriptions release during app shutdown.<br>Live acceptance evidence (2026-03-14): signed-in browser session remained valid during `make dev-deploy`; `/devices` no longer flashed empty homepage solar data during rollout; `/profile` remained stable; and after `docker restart $(docker ps --filter 'name=k3d-pulse-local' -q)` both `make platform-wait` and `make services-wait` completed successfully with the cluster recovering cleanly and no manual pod intervention required. | M1 schema + auth |
| DONE | Local platform cold-start recovery hardening<br>- [x] Detect and surface k3d node readiness failures before platform waits claim success<br>- [x] Strengthen local dependency gates for CNPG, NATS, Valkey, and Keycloak readiness after Docker/Kubernetes restarts<br>- [x] Add startup retry/backoff for dependency-bound services so cold starts recover without widespread CrashLoopBackOff<br>- [x] Add a documented local recovery command/path for stranded stateful pods after host restarts<br>- [x] Capture acceptance evidence from a full cold-start recovery drill<br>- [x] Switch local Valkey from ephemeral memory-only storage to PVC-backed AOF persistence with Sentinel recovery settings<br>- [x] Validate Valkey cache/snapshot keys survive a full cold restart on retained PVCs<br>Validation evidence (2026-03-14): `make platform-recover-local` succeeded after tightening `platform-wait` to require real core endpoint readiness and changing `services-wait` to validate deployment-level ready/available replica health instead of flaky pod-level waits; the local cluster recovered without manual pod intervention.<br>Validation evidence (2026-03-14): `kubectl --context k3d-pulse-local -n pulse-platform exec statefulset/pulse-platform-valkey-node -- sh -lc 'valkey-cli -p 6379 SET pulse:test:coldrestart ok'` returned `ok`; after `docker restart $(docker ps --filter "name=k3d-pulse-local" -q)` and `make platform-recover-local`, `kubectl --context k3d-pulse-local -n pulse-platform exec statefulset/pulse-platform-valkey-node -- sh -lc 'valkey-cli -p 6379 GET pulse:test:coldrestart'` returned `ok`, confirming PVC-backed Valkey cold-restart durability. | M0 platform baseline |

**Acceptance criteria**
- [x] Social login flow implemented (Expo PKCE via Keycloak OIDC in universal settings)
- [x] Device list APIs return only owned/shared devices (`ListUserDevices`, provider-grouped `ListDevices`)
- [x] Forged access rejected by subject-boundary and RBAC checks in gRPC control plane

**Post-M1 follow-up (explicitly deferred until M1 is DONE)**
- [x] Add GitHub `db-migrations-ci` workflow (up/verify/down/up/e2e) and make it a required check.
- [x] DONE: Define and implement schema migration rollout path for `dev -> staging -> prod` (Argo sync hook / migration job sequencing, backup gates, forward-only policy).
  - [x] Review architecture/config/ADR constraints for GitOps-driven environment rollout
  - [x] Add a forward-only migration runner for in-cluster rollout jobs
  - [x] Wire migration job sequencing into `pulse-services` Helm/Argo deploy flow
  - [x] Add explicit dev/staging/prod rollout policy + backup gate configuration/docs
  - [x] Validate render/test path and record acceptance evidence
  - Validation evidence (2026-03-09): `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml` succeeded; `helm lint deploy/charts/pulse-services -f deploy/env/dev/values.services.yaml` succeeded; `go test ./...` succeeded; `make lint` succeeded.
- [x] DONE: Adopt `pgroll` for safe reversible online PostgreSQL schema migrations with simultaneous multi-schema serving during transitions.
  - [x] Add minimal repo adoption: local `pgroll` Make targets, plan directory, and transition documentation
  - [x] Keep the current raw SQL rollout job as the active environment path until runtime/search-path versioning exists
  - [x] Validate minimal adoption path (`make -n pgroll-init-local`, `make -n pgroll-start-local PGROLL_PLAN=deploy/db/pgroll/plans/example.json`, `make lint`)
  - [x] Review the runtime DB touch surface (`CONTROL_PLANE_DB_DSN`, `ARCHIVE_MANIFEST_DB_DSN`, pgx pools) for search-path cutover wiring
  - [x] Add a shared Postgres search-path helper and regression tests
  - [x] Wire Go service/runtime DB access to version-aware schema cutover so simultaneous old/new schema serving is real, not only planned
  - [x] Expose the runtime schema-selection contract in charts/docs and record validation evidence
  - Validation evidence (2026-03-09): `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml` succeeded; `helm lint deploy/charts/pulse-services -f deploy/env/dev/values.services.yaml` succeeded; `go test ./...` succeeded; `make lint` succeeded.
- [x] DONE: Keep M1 implementation local-first: continue validating schema changes in local k3d/CNPG before enabling environment rollout automation.
  - [x] Local-first gating is explicit in `docs/how-to/rollout-schema-migrations-dev-staging-prod.md`
  - [x] pgroll transition docs keep local validation as the prerequisite path before any env rollout
  - [x] `docs/reference/commands.md` now calls out `make test-db-migrations-ci` as the CI-aligned local gate
  - Validation evidence (2026-03-09): `make lint` succeeded.
- [x] DONE: Add Helm/Make startup sequencing hardening for Keycloak bootstrap dependencies to reduce first-boot restart/transient not-ready events.
  - [x] Review current `platform-up` / `platform-wait` flow and Keycloak chart bootstrap resources
  - [x] Defer first-pass Keycloak apply until CNPG/bootstrap prerequisites are ready
  - [x] Wait for Keycloak config-cli bootstrap job completion when present
  - [x] Update local bringup/docs and record validation evidence
  - Validation evidence (2026-03-09): `rg -n "fresh Keycloak bootstrap detected|keycloak_bootstrap_override|wait_secret|keycloak_first_pass_flags" Makefile` showed the first-pass Keycloak deferral path; `rg -n "condition=complete job/|keycloak-keycloak-config-cli" Makefile` showed the config-cli completion wait; `make lint` succeeded.

---

## M2 — Telemetry pipeline v1 + archive + replay
**Execution sequence (M2 closeout order)**

- [x] Step 0: `TelemetryEnvelope` + deterministic NATS subject/shard model
- [x] Step 1: Ingest durability hardening (JetStream ingest stream bootstrap + publish retry/backpressure policy)
- [x] Step 2: Projection pipeline (`pulse.telemetry.ingest.*` → Valkey live snapshot)
  - [x] Added projection worker baseline (`cmd/ecoflow-projection-worker`)
  - [x] Added JetStream queue consumer on ingest wildcard subjects with durable/ack tuning
  - [x] Added envelope projection (numeric metrics extraction + stale/duplicate guard)
  - [x] Added Valkey live snapshot store with cluster-aware key tags and cursor sequence
  - [x] Added explicit read-side snapshot contract (`SnapshotReader` / `SnapshotReadModel`) for downstream realtime/query consumers
  - [x] Wired gRPC `TelemetryService.GetSnapshot` to use Valkey-backed snapshot reader contract when configured
  - [x] Added end-to-end checkpoint/recovery tests (worker restart + stale replay idempotency + cursor monotonicity)
  - [x] Wire snapshot contract into downstream query/realtime streaming APIs (`TelemetryService.Subscribe` now emits delta/heartbeat from the same Valkey `SnapshotReadModel` contract)
- [x] Step 3: Raw archive writer (protobuf+zstd objects to MinIO/GCS)
  - [x] Added archive worker baseline (`cmd/ecoflow-archive-worker`)
  - [x] Added JetStream queue consumer on ingest wildcard subjects with durable/manual-ack semantics
  - [x] Added shard/hour object partitioning with protobuf length-delimited frames + zstd compression
  - [x] Added MinIO-compatible object writer (`internal/archiveworker`) with metadata/checksum tagging
  - [x] Added archive worker docs + Make target (`make archive-worker`)
  - [x] Add object-store integration tests (MinIO/GCS parity + failure injection)
- [x] Step 4: Manifest index persistence (Postgres metadata for replay lookup)
  - [x] Added migration `000003_m2_archive_manifest_schema` with UUIDv7 primary key, app-managed UTC timestamps, object uniqueness, and replay lookup indexes
  - [x] Added Postgres manifest store (`internal/archiveworker/manifest_store.go`) with idempotent upsert on `(object_bucket, object_key)`
  - [x] Wired archive writer flush path to persist manifest metadata after object write and before ACK
  - [x] Added unit coverage for manifest normalization/validation and manifest write failure (NAK/retry path)
  - [x] Extended local migration verification to include manifest table + constraints
- [x] Step 5: Replay CLI (device list + shard/time replay modes)
  - [x] Added replay CLI command (`cmd/ecoflow-replay-cli`) with `list-devices`, `device`, and `fleet` modes
  - [x] Added replay runtime package (`internal/replaycli`) with manifest query, object fetch, frame decode, and publish orchestration
  - [x] Added manifest-backed device/provider-id listing in time window for safe replay targeting
  - [x] Added per-device replay filtering (`device_ids` and/or `provider_device_ids`) and fleet replay shard/time filtering
  - [x] Added replay NATS publishing through shared `internal/telemetrybus` subject helpers (`ReplaySubject`)
  - [x] Added dry-run mode for decode/filter validation without NATS publish side-effects
  - [x] Added replay unit tests for decoder + runner filtering/error paths and command/docs wiring
- [x] Step 6: Gap detector + replay enqueue workflow
  - [x] Added gap-repair request protobuf contract (`proto/pulse/replay/v1/replay.proto`) + generated stubs (`gen/pulse/replay/v1`)
  - [x] Added gap-repair stream bootstrap helper (`internal/telemetrybus/jetstream_gaprepair.go`) with deterministic wildcard subject coverage
  - [x] Added gap detector runtime package (`internal/gaprepair/detector.go`) that compares projection cursor vs archive manifest coverage and enqueues targeted replay windows
  - [x] Added gap-repair worker runtime package (`internal/gaprepair/worker.go`) that consumes queue jobs and replays back into ingest subjects
  - [x] Added runnable commands (`cmd/ecoflow-gap-detector`, `cmd/ecoflow-gap-repair-worker`) + Make targets (`make gap-detector`, `make gap-repair-worker`)
  - [x] Added unit coverage for detector candidate ordering/window planning and worker ack/nak/term behavior

| Status | Task | Dependency |
|---|---|---|
| DONE | `TelemetryEnvelope` protobuf + versioning<br>- [x] Added canonical ingest/archive envelope schema at `proto/pulse/envelope/v1/envelope.proto`<br>- [x] Added explicit version fields (`envelope_version`, `payload_version`) and source/encoding enums<br>- [x] Added deterministic shard metadata fields (`shard`, `shard_count`) to support replay partitioning<br>- [x] Generated Go stubs via Buf (`gen/pulse/envelope/v1`)<br>- [x] Wired envelope builder into ingest path (`MQTT -> TelemetryEnvelope -> NATS`) via `internal/ingestworker/envelope_builder.go` | M0 |
| DONE | NATS subject model + sharding rules<br>- [x] Added deterministic shard function (`ShardForDevice`) using stable FNV-1a hashing in `internal/telemetrybus`<br>- [x] Added versioned subject taxonomy helpers for ingest/projection/archive/replay/gap-repair subjects<br>- [x] Added regression tests for subject formatting + shard determinism<br>- [x] Added NATS envelope publisher and shared publish helper (`internal/telemetrybus/publisher.go`) for ingest subjects<br>- [x] Wired projection worker subscription to shared `internal/telemetrybus` wildcard ingest subject helper<br>- [x] Wired replay CLI publishing to shared `internal/telemetrybus` replay subject helper | M0 |
| DONE | Distributed MQTT ingest worker pool + global lease locks (ADR-0014)<br>- [x] Locked one-session-per-`(provider, provider_device_id)` rule for distributed workers<br>- [x] Locked Valkey lease protocol baseline (`TTL=45s`, `heartbeat=15s`, tuned by soak testing)<br>- [x] Locked graceful drain behavior for deactivation/credential disable events (event-driven)<br>- [x] Implement provider adapter contract (`EcoFlow` first) for connect/reconnect certification flow<br>  - [x] Added lean real EcoFlow adapter (`internal/provideradapter`) with `DiscoverDevices` + per-device certification guard<br>  - [x] Wired `cmd/ecoflow-grpc-api` to register the real EcoFlow discoverer by default<br>  - [x] Added unit tests for adapter mapping/validation/certification behavior and opt-in seeded integration verification<br>- [x] Implement Valkey Lua lease manager (acquire/renew/release with token + fencing)<br>  - [x] Added ADR-0014 keying implementation (`lease/session/fence`) with cluster hash-tag safety in `internal/ingestlease`<br>  - [x] Added token-checked Lua scripts for acquire/renew/release + graceful drain support (`TTL=45s`, `heartbeat=15s` defaults)<br>  - [x] Added `valkey-go` client bootstrap tuned for lease path resilience (cluster refresh + MOVED/ASK handling + cache explicitly disabled for locks)<br>  - [x] Added concurrency-heavy unit tests (single-winner contention, fencing increment, heartbeat drain/release lifecycle)<br>- [x] Implement worker assignment loop and session lifecycle manager (multi-replica safe)<br>  - [x] Added internal assignment poller + reconciler (`internal/ingestworker`) keyed by `(provider, provider_device_id)`<br>  - [x] Polls `provider_devices` and starts sessions only when `is_active=true`, `credential.is_active=true`, and `ingest_desired_state=active`<br>  - [x] Claims global lease before session start and maintains lease heartbeat until session stop<br>  - [x] Stops sessions cleanly on `draining` / `paused` / credential disable / device disable / assignment removal<br>  - [x] Recycles running sessions when assignment credential id or key material changes so DB credential updates are applied without pod restarts<br>  - [x] Treats EcoFlow business error `code=8513` (`accessKey is invalid`) as a terminal session error to force control-plane credential refresh on next reconcile<br>  - [x] Added runnable distributed worker entrypoint (`cmd/ecoflow-ingest-worker`)<br>  - [x] Added concrete startup pool defaults (`start_workers=clamp(4*GOMAXPROCS,8,64)`, `start_queue_size=start_workers*8`) with env overrides (`INGEST_START_WORKERS`, `INGEST_START_QUEUE_SIZE`)<br>  - [x] Added recommended HPA policy manifest for ingest workers (`deploy/env/dev/recommended/pulse-services-go-ingest-hpa.recommended.yaml`)<br>- [x] Replaced production ingest path host-local lock dependency with distributed Valkey lease ownership (host-local lock remains only in legacy CLI tooling) | M0 + ADR-0014 |
| DONE | Containerized telemetry worker runtime (`pulse-services` chart)<br>- [x] Added Kubernetes Deployments for `go-ingest`, `go-projection`, and `go-archive`<br>- [x] Added shared runtime ConfigMap/Secret for worker env wiring (DB/NATS/Valkey/MinIO)<br>- [x] Added local worker image build/import flow (`deploy/docker/pulse-services.Dockerfile`, `make services-image-*`)<br>- [x] Updated local services values to run telemetry pipeline in-cluster by default (no local `go run` path required) | M2 |
| DONE | Ingest: MQTT → normalize → NATS<br>- [x] Ingest worker now resolves MQTT certification per connect/reconnect and opens one MQTT session per leased provider device<br>- [x] MQTT payloads are normalized into `TelemetryEnvelope` (UUIDv7 envelope id, shard metadata, labels, payload encoding, source/type mapping)<br>- [x] Envelopes are published to `pulse.telemetry.ingest.sNNN` via `internal/telemetrybus` NATS publisher<br>- [x] Added session-runner/publisher/envelope unit tests to prevent ingest regressions<br>- [x] Step 1.1 JetStream stream bootstrap (`PULSE_TELEMETRY_INGEST`) from worker startup<br>- [x] Step 1.2 NATS publish retry/backpressure policy (bounded timeout + jittered retries)<br>- [x] Step 1.3 Worker env knobs/docs for JetStream bootstrap + publish retry tuning<br>- [x] Step 1.4 Failure-mode tests (stream add/update/no-op + retry success/exhaust/cancel) | proto + NATS |
| DONE | EcoFlow quota bootstrap + metadata refresh<br>- [x] Pull `GetDeviceAllQuota()` on session start before/alongside MQTT steady-state consumption<br>- [x] Refresh quota periodically with jittered polling and on MQTT-stale/reconnect paths<br>- [x] Normalize quota into the same telemetry `params` shape as MQTT and publish to `pulse.telemetry.ingest.sNNN` with `source=quota`<br>- [x] Persist quota-derived `provider_devices.capabilities` and `provider_devices.metadata` during ingest<br>- [x] Surface quota-derived capabilities/details through control-plane + public device read APIs without provider-specific parsing in the UI<br>- [x] Archive normalized quota frames so replay/rebuild paths can reproduce quota-derived solar behavior (ADR-0015 supersedes prior archive exclusion rule)<br>- [x] Add unit coverage for the normalizer, session bootstrap/poll path, archive handling, and read-path mapping | ADR-0014 + ADR-0015 + config-05 |
| DONE | Projection: NATS → Valkey live snapshot<br>- [x] Added projection worker command (`cmd/ecoflow-projection-worker`) for distributed consumer runtime<br>- [x] Added JetStream queue consumer with durable/manual-ack semantics on ingest wildcard subjects<br>- [x] Added telemetry projection store (`internal/projectionworker`) that merges numeric metrics into per-device live snapshots<br>- [x] Added stale/duplicate envelope guards (envelope id + ingest timestamp checks)<br>- [x] Added cluster-aware Valkey keying for atomic snapshot/cursor groups (`{did:*}` / `{sn:*}` tags)<br>- [x] Added explicit downstream read-model contract (`SnapshotReader` / `SnapshotReadModel`) and wired gRPC snapshot reads to it when Valkey is configured<br>- [x] Added projection replay/gap-repair contract validation + end-to-end checkpoint/recovery tests (worker restart + stale replay idempotency + cursor monotonicity)<br>- [x] Added downstream query/realtime stream integration over the same snapshot read-model contract (`TelemetryService.Subscribe`) | NATS + Valkey |
| DONE | Archive writer: protobuf+zstd objects<br>- [x] Added archive worker runtime (`cmd/ecoflow-archive-worker`)<br>- [x] Added archive pipeline package (`internal/archiveworker`) with JetStream durable consumer + graceful drain<br>- [x] Added shard/hour object keying (`raw/yyyy=.../hh=.../shard=.../part-...pb.zst`)<br>- [x] Added length-delimited protobuf framing + zstd compression and per-object metadata/checksum<br>- [x] Added unit coverage for ack/nak/term paths, interval flush, and frame decode verification<br>- [x] Added object-store integration tests (MinIO contract + failure injection, parity-ready harness) | storage |
| DONE | Manifest index table (Postgres)<br>- [x] Added `archive_object_manifest` schema migration with replay lookup indexes<br>- [x] Added archive manifest upsert store and worker wiring<br>- [x] Added unit tests for manifest normalization + flush failure semantics<br>- [x] Added local migration verify checks for manifest constraints | M1 DB |
| DONE | Replay CLI: device list + shard/time replay<br>- [x] Added replay CLI command and runtime package (`cmd/ecoflow-replay-cli`, `internal/replaycli`)<br>- [x] Added manifest-backed list-devices mode for safe target discovery<br>- [x] Added per-device and per-fleet replay modes with shard/time filters and dry-run support<br>- [x] Added replay decoder/runner tests and command/docs coverage | archive + manifest |
| DONE | Gap detector + replay enqueue<br>- [x] Added replay queue request schema (`GapRepairRequest`) with deterministic shard metadata and replay window fields<br>- [x] Added NATS JetStream gap-repair stream bootstrap and wildcard subject helpers<br>- [x] Added detector service (`internal/gaprepair`) that polls active ingest assignments, compares projection cursor vs archive coverage, and enqueues targeted replay jobs<br>- [x] Added queue consumer worker (`internal/gaprepair`) that drains gap-repair jobs and invokes replay in device-scoped windows<br>- [x] Added runtime commands + Make targets (`ecoflow-gap-detector`, `ecoflow-gap-repair-worker`) for local/dev execution<br>- [x] Added unit tests for detector planning/order + worker replay ack/nak/term paths | projections |

**Validation evidence (local k3d, 2026-02-26 UTC)**
- [x] Platform + services ready: `make platform-wait services-wait` reports all pods `Running/Ready` in `pulse-platform` and `pulse-services`.
- [x] Ingest sessions active: `kubectl -n pulse-services logs deploy/pulse-services-go-ingest --since=2m` shows `ecoflow ingest session connected` for both seeded SNs and continuous async queue metrics with zero drops.
- [x] NATS ingest stream healthy: `nats stream info PULSE_TELEMETRY_INGEST` reports live growth (`67,740` messages, two active consumers) and `nats consumer ls PULSE_TELEMETRY_INGEST` shows archive/projection consumers current.
- [x] Projection persisted to Valkey: `valkey-cli GET pulse:projection:{did:...}:snapshot` returns live JSON snapshot with cursor fields (`cursor_seq`, `cursor_ts_unix_ms`).
- [x] Archive persisted to MinIO + manifest index: `archive_object_manifest` has rows (`COUNT(*) = 78`) and MinIO object paths exist under `/export/pulse-telemetry-raw/raw/yyyy=2026/mm=02/dd=26/hh=22/shard=*`.
- [x] Replay CLI validated against manifest/object storage:
  - `go run ./cmd/ecoflow-replay-cli -mode list-devices ...` returns both seeded provider device IDs.
  - `go run ./cmd/ecoflow-replay-cli -mode device ... -dry-run` completed with `objects_processed=19`, `messages_decoded=1756`, `messages_failed=0`.
- [x] Targeted gap repair proven end-to-end:
  - Paused projection worker (`replicas=0`), removed one snapshot key, ran detector dry-run and observed planned replay jobs.
  - Ran detector non-dry-run and observed `enqueued=2` to `PULSE_TELEMETRY_GAPREPAIR`.
  - Ran gap-repair worker and observed `gap-repair replay completed` for both provider device IDs.
  - Verified queue drained (`PULSE_TELEMETRY_GAPREPAIR Messages: 0`) and restored projection worker (`replicas=1`).

**Acceptance criteria**
- [x] Replay can rebuild live state for last 24h.
- [x] Targeted gap repair works without replaying everything.

---

## M3 — Rollups + history queries + comparisons
| Status | Task | Dependency |
|---|---|---|
| DONE | Timescale hypertables + indexes<br>- [x] Added M3 rollup migration scaffold (`000004_m3_rollups_hypertables_schema.up/.down.sql`)<br>- [x] Added rollup hypertables (`telemetry_rollup_minute`, `telemetry_rollup_hour`, `telemetry_rollup_day`)<br>- [x] Added read/query indexes for device and provider-device time-window scans<br>- [x] Extended local migration verification to assert rollup table + hypertable presence<br>- [x] Validated migration cycle (`make db-migrate-cycle-local`) and e2e checks (`make db-migrate-e2e-local`) | M0 |
| DONE | Rollup pipeline (minute/hour/day)<br>- [x] Added `internal/rollupworker` extraction/store pipeline for minute/hour/day rollup upserts<br>- [x] Added `cmd/ecoflow-rollup-worker` runtime, Docker build target, Make target, and Helm worker deployment scaffolding<br>- [x] Added unit tests for payload extraction, worker delivery semantics, and Postgres upsert transaction flow<br>- [x] Validated end-to-end local rollup writes against the live ingest stream and recorded evidence<br>  - [x] Fresh local baseline started at `minute=0`, `hour=0`, `day=0` before the worker ran<br>  - [x] Local `go run ./cmd/ecoflow-rollup-worker` consumed the live JetStream ingest stream via forwarded NATS/Postgres endpoints<br>  - [x] Post-run rollup state advanced to `minute=13`, `hour=2`, `day=2`, with `distinct_devices=2` and latest bucket `2026-02-27T12:30:00Z`<br>  - [x] JetStream consumer `rollup-timeseries-v1` reached `ack floor == delivered` with zero outstanding acks before graceful stop | M2 |
| DONE | Retention jobs: minute 90d, hour/day 3y<br>- [x] Added Timescale retention migration scaffold (`000005_m3_rollup_retention_policies.up/.down.sql`)<br>- [x] Added retention policies for minute=`90 days`, hour=`3 years`, day=`3 years`<br>- [x] Extended local migration verification to assert retention policy presence<br>- [x] Validated migration cycle locally with `make db-migrate-cycle-local` and confirmed policy rows:<br>&nbsp;&nbsp;`telemetry_rollup_minute&#124;90 days&#124;1 day`<br>&nbsp;&nbsp;`telemetry_rollup_hour&#124;3 years&#124;1 day`<br>&nbsp;&nbsp;`telemetry_rollup_day&#124;3 years&#124;1 day` | Timescale |
| DONE | Go gRPC query APIs (range + compare)<br>- [x] Extend `TelemetryService` protobuf contract with typed rollup range + compare RPCs<br>- [x] Add Timescale-backed rollup query reader package (`internal/telemetryquery`)<br>- [x] Enforce device-level authz on telemetry queries inside `ecoflow-grpc-api`<br>- [x] Validate local hourly/range reads end-to-end against live rollup data and capture evidence<br>  - [x] Started `cmd/ecoflow-grpc-api` locally against forwarded CNPG using `CONTROL_PLANE_DB_DSN='host=127.0.0.1 port=15432 user=pulse password=*** dbname=pulse sslmode=disable' GRPC_LISTEN_ADDR='127.0.0.1:9090' GRPC_AUTH_MODE='noop' go run ./cmd/ecoflow-grpc-api`<br>  - [x] Queried `QueryRollupRange` for hourly bucket `[2026-02-27T12:00:00Z, 2026-02-27T13:00:00Z)` for device `019c9f0e-4521-775d-873e-e80039f16d75` (D2M) and got `samples=315`, `ac_in_avg=155.69W`, `pv_avg=0.00W`, `load_avg=205.23W`, `battery_avg=52.61W`, `soc_avg=20.49%`<br>  - [x] Queried `QueryRollupRange` for the same hourly bucket for device `019c9f0e-452b-70eb-b3af-1c0f15c34416` (DPU) and got `samples=31`, `ac_in_avg=6.76W`, `pv_avg=22.63W`, `load_avg=0.00W`, `battery_avg=-87.52W`, `soc_avg=54.81%`<br>  - [x] Queried `CompareRollupRange` for device `019c9f0e-452b-70eb-b3af-1c0f15c34416`; current window returned `1` point and previous period window `[2026-02-27T11:00:00Z, 2026-02-27T12:00:00Z)` returned `0` points<br>  - [x] Service logs showed successful unary calls for `/pulse.telemetry.v1.TelemetryService/QueryRollupRange` and `/pulse.telemetry.v1.TelemetryService/CompareRollupRange` with `grpc.code=OK` | rollups |
| DONE | Node REST endpoints → gRPC<br>- [x] Chosen implementation shape: dedicated Node BFF workspace app under `apps/` using JWT validation + gRPC forwarding<br>- [x] Scaffolded `apps/pulse-platform` Node REST BFF runtime, workspace wiring, and frontend CI coverage<br>- [x] Added telemetry history REST endpoints for range + compare queries backed by `TelemetryService`<br>- [x] Forwarded bearer JWT to gRPC metadata and validated local/dev auth behavior (`noop` + bearer pass-through)<br>- [x] Added Node tests for request validation, gRPC mapping, and endpoint responses<br>- [x] Validated local end-to-end REST → gRPC → Timescale flow and captured evidence<br>  - [x] Started local Node BFF with `GRPC_API_ADDR='127.0.0.1:9090' NODE_AUTH_MODE='noop' PULSE_PLATFORM_HOST='127.0.0.1' PULSE_PLATFORM_PORT='18081' npm run platform-bff`<br>  - [x] `GET /healthz` returned `{\"ok\":true}`<br>  - [x] `GET /api/v1/devices/019c9f0e-4521-775d-873e-e80039f16d75/history?resolution=hour&from=2026-02-27T12:00:00Z&to=2026-02-27T13:00:00Z` returned one hourly rollup point with `sampleCount=315`, `acInAvgW=155.69`, `loadAvgW=205.23`, `batteryAvgW=52.61`, `socAvgPct=20.49`<br>  - [x] `GET /api/v1/devices/019c9f0e-452b-70eb-b3af-1c0f15c34416/history/compare?resolution=hour&from=2026-02-27T12:00:00Z&to=2026-02-27T13:00:00Z` returned device-specific `current` and `previous` series, with current `sampleCount=31`, `pvAvgW=22.63`, `batteryAvgW=-87.52`, and an empty previous window | gRPC |

**Acceptance criteria**
- All time windows supported
- Prior-period comparison returned in a single call (server-side)

**Milestone closeout note (M1-M3)**
- M1, M2, and M3 acceptance requirements are satisfied and the milestones are considered closed.
- Any remaining follow-ups listed in this README or related ADRs are accepted technical debt or deferred hardening items; they do not block milestone closure or M4 execution unless they are explicitly promoted back into a milestone acceptance gate.

---

## M4 — WebSockets gateway + UX hardening
| Status | Task | Dependency |
|---|---|---|
| DONE | WS Gateway (auth, authz, subscribe protocol) | M1 |
 - [x] Enforce device-level authz on live gRPC snapshot/subscribe APIs
 - [x] Add dedicated WS gateway service with JWT/noop auth modes
 - [x] Preserve existing Expo subscribe/unsubscribe/ping message contract
 - [x] Bridge snapshot-first + delta stream for a subscribed device
 - [x] Add authz/reconnect/contract tests for the first thin slice
| DONE | Snapshot-on-connect (Valkey) + deltas (NATS) | M2 |
 - [x] Read current device snapshot from Valkey projection keys on subscribe
 - [x] Subscribe to shared NATS telemetry subjects and fan out per-device deltas
 - [x] Buffer deltas/heartbeats until snapshot delivery completes, then flush in order
 - [x] Keep live authz at the Go boundary while moving the data plane off direct gRPC streaming
| DONE | Backpressure + downsampling ladder | WS |
 - [x] Add per-device delivery lanes with staged intervals (`250ms -> 500ms -> 1s`)
 - [x] Degrade to `key-only` delivery under sustained pressure
 - [x] Pause delivery entirely under extreme pressure while continuing to merge state
 - [x] Recover automatically after quiet ticks and cover escalation/recovery in unit tests
| DONE | Expo client WS integration + reconnection UX<br>- [x] Added auth-aware Expo telemetry session hook (`useAuthSession`) so REST queries and WS connect wait for persisted PKCE session hydration before starting<br>- [x] Moved WS lifecycle ownership into `TelemetryEngineProvider` so token refresh/reconnect preserves active device subscriptions instead of reconnecting from each screen hook<br>- [x] Added explicit `auth_required` telemetry engine state and sign-in-required devices UX when OIDC is configured but no valid access token exists<br>- [x] Scoped Expo device queries by auth session key to prevent cross-session cache bleed while preserving mock polling behavior<br>- [x] Added `TelemetryEngine` regression tests covering `auth_required` and token-change reconnect behavior<br>- [x] Validated the universal app end-to-end against the realtime gateway (`apps/pulse-realtime-gateway`) in local/dev, including `/devices` and canonical `/device/<uuid>` live subscriptions without serial-number route parameters<br>- [x] Confirmed snapshot-first websocket delivery from Valkey + NATS deltas for both seeded devices in local/dev | WS |

**Acceptance criteria**
- 250ms updates when possible
- Graceful degradation under pressure
- Robust reconnect/resubscribe

**M4 closeout note**
- M4 is complete. The live WebSocket path is validated in local/dev end-to-end: Valkey snapshot on subscribe, NATS delta fanout, staged backpressure/downsampling, Expo reconnect/resubscribe, and canonical `/device/<uuid>` subscriptions without exposing serial-number route parameters in the UI.
- Remaining testing/operability work moves to M5 and does not block M4 closure unless explicitly promoted back into an M4 acceptance gate.

---

## M5 — Testing + operability + DR-lite
| Status | Task | Dependency |
|---|---|---|
| DONE | Integration tests (Testcontainers: Postgres/Valkey/NATS/MinIO)<br>- [x] Define first slice scope: Go integration harness for control-plane/history/realtime dependencies<br>- [x] Add reusable Testcontainers helpers for Postgres, Valkey, NATS, and MinIO lifecycle<br>- [x] Add first end-to-end Go integration suite against the core data path (NATS -> projection/archive or rollup query path)<br>- [x] Wire the suite into local/CI commands and document how developers run it<br>- [x] Validation: `make test-pipeline-integration` | M2 |
| DONE | Contract tests (Node↔Go proto compatibility)<br>- [x] Add shared Node↔Go protobuf contract fixture generator from Go types<br>- [x] Add realtime gateway contract tests that decode Go-generated envelope bytes and validate field mapping semantics<br>- [x] Wire contract tests into local commands and CI execution path<br>- [x] Record validation evidence and mark task DONE<br>- [x] Validation: `make test-proto-contract` | M1/M2 |
| DONE | E2E web (Playwright)<br>- [x] Mark task `PROGRESS` and define deterministic E2E scope (mocked API at browser boundary)<br>- [x] Add Playwright web config + route-mocked specs for `/devices` and `/device/{uuid-or-sn}`<br>- [x] Wire local command + CI check and record validation evidence<br>- [x] Validation: `make test-web-e2e` | M4 |
| DONE | E2E mobile smoke (Maestro)<br>- [x] Mark task `PROGRESS` and define deterministic mobile smoke scope (app boot + tab navigation + static assertions)<br>- [x] Add Maestro flow + runner wiring (`apps/universal/e2e/maestro/smoke.yaml`, `npm run -w apps/universal e2e:mobile`, `make test-mobile-e2e`)<br>- [x] Document prerequisites/commands and record current blocker evidence (`make test-mobile-e2e` fails fast with explicit `Java runtime not found` preflight)<br>- [x] Validate full simulator/emulator smoke run end-to-end (`make test-mobile-e2e` passes) and capture acceptance evidence<br>  - [x] Validation evidence (2026-03-05): `make test-mobile-e2e` passed on iOS simulator (`iPhone 17 Pro`, `iOS 26.2`) with full tab-navigation smoke flow completion | M4 |
| DONE | ADR-0017 local HTTP/3 verification automation<br>- [x] Mark task `PROGRESS`, confirm local ingress/TLS/HTTP3 dependencies are complete, and capture scope (make target + toolchain guard + docs)<br>- [x] Add a local automation step that verifies HTTP/3 with an in-repo/toolchain client when HTTP/3 ingress is enabled<br>- [x] Keep the verification gated behind local HTTP/3 enablement and clear toolchain preflight checks<br>- [x] Validation: run the new local verification command against the documented localhost ingress path<br>  - [x] Validation evidence (2026-03-10): `make edge-verify-http3-local` failed fast with `curl is installed, but the linked libcurl lacks HTTP/3 support; install an HTTP/3-capable curl before running this check.`<br>  - [x] Validation evidence (2026-03-10): `make lint` succeeded | ADR-0017 |
| DONE | ADR-0017 dev/GKE HTTP/3 default policy<br>- [x] Mark task `PROGRESS`, confirm local/browser-edge HTTP/3 behavior is already wired, and capture scope (policy decision + config/docs alignment)<br>- [x] Decide whether dev/GKE should keep HTTP/3 opt-in or become the default<br>- [x] Align deploy/config/reference docs and values commentary with the chosen policy<br>- [x] Validation: `make lint`<br>  - [x] Validation evidence (2026-03-10): `make lint` succeeded | ADR-0017 |
| DONE | ADR-0014 ingest autoscaling custom metrics<br>- [x] Mark task `PROGRESS`, confirm ADR-0014 ingest dependencies are complete, and capture scope (metrics emission + scrape path + KEDA-ready scaling manifest)<br>- [x] Add ingest worker custom metrics for `ingest_unassigned_active_devices`, `ingest_reconcile_duration_p95_seconds`, and `ingest_lease_acquire_latency_p95_seconds`<br>- [x] Expose ingest worker metrics over an in-cluster scrape endpoint and chart wiring<br>- [x] Add recommended KEDA/Prometheus scaling manifest and operator docs for the custom metrics pipeline<br>- [x] Validation: targeted Go tests plus chart/render validation for the ingest metrics path<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/ingestworker -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/ingestworker  0.828s`<br>  - [x] Validation evidence (2026-03-10): `go test ./...` -> `ok` across repo, including `cmd/ecoflow-grpc-api`, `internal/ingestworker`, and `internal/integrationtest`<br>  - [x] Validation evidence (2026-03-10): `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml` succeeded<br>  - [x] Validation evidence (2026-03-10): `helm lint deploy/charts/pulse-services -f deploy/env/dev/values.services.yaml` succeeded<br>  - [x] Validation evidence (2026-03-10): `make lint` succeeded | ADR-0014 |
| DONE | Generalize provider adapters beyond EcoFlow<br>- [x] Mark task `PROGRESS`, confirm ADR-0014 dependencies are complete, and capture scope (registry-backed discovery/session adapter wiring)<br>- [x] Add provider-agnostic adapter contracts + registry in `internal/provideradapter`<br>- [x] Route control-plane discovery/provider validation through the registry instead of EcoFlow-specific wiring<br>- [x] Route ingest session dispatch through provider-specific runner registration instead of a single EcoFlow runner type<br>- [x] Validation: targeted Go tests for provider adapter registry, control-plane discovery, and ingest session dispatch<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/provideradapter -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/provideradapter  0.412s`<br>  - [x] Validation evidence (2026-03-10): `go test ./cmd/ecoflow-grpc-api -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/cmd/ecoflow-grpc-api  1.514s`<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/ingestworker -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/ingestworker  0.524s` | ADR-0014 |
| DONE | ADR-0014 ingest chaos tests<br>- [x] Mark task `PROGRESS`, confirm dependency `M2` is complete, and capture scope (lease loss, worker crash handoff, reconnect storms)<br>- [x] Add ingest-loop chaos tests for lease loss recovery and cross-worker lease handoff<br>- [x] Add EcoFlow session-runner reconnect-storm serialization coverage<br>- [x] Validation: `go test ./internal/ingestworker -count=1`<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/ingestworker -run '^TestLoopChaosReacquiresAfterLeaseLoss$' -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/ingestworker  0.794s`<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/ingestworker -run '^TestLoopChaosWorkerCrashHandsLeaseToPeer$' -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/ingestworker  1.145s`<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/ingestworker -run '^TestEcoFlowSessionRunnerChaosSerializesReconnectStorm$' -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/ingestworker  0.410s`<br>  - [x] Validation evidence (2026-03-10): `go test ./internal/ingestworker -count=1` -> `ok   github.com/jpaljasma/ecoflow-pulse/internal/ingestworker  0.741s` | M2 |
| DONE | Load tests (k6): ingest + WS + query<br>- [x] Mark task `PROGRESS`, confirm dependency `M4` is complete, and capture kickoff scope (ingest publish, WS fanout, query read path)<br>- [x] Add k6 scenarios + shared fixtures for ingest, websocket, and query coverage (`load/k6/main.js`, `load/k6/lib/*`, `load/k6/scenarios/*`)<br>- [x] Wire reproducible local command(s) + docs for load execution and thresholds (`make test-load-k6`, `cmd/ecoflow-loadtest-ingest-bridge`, `docs/reference/commands.md`)<br>- [x] Record validation evidence, close checklist, and mark task `DONE`<br>  - [x] Validation evidence (2026-03-05): `make test-load-k6` passed on local `k3d-pulse-local` (`history_query` p95 `6.16ms`, `ingest_publish` p95 `5.14ms`, `ws_session_success` `100%`, `ingest_accepted_total=1201`, `query_success_total=61`)<br>- [x] Validation: `make test-load-k6` | M4 |
| DONE | Backups/restore drills + runbooks<br>- [x] Mark task `PROGRESS`, confirm dependencies (`M0`, `M1`) are complete, and capture kickoff scope<br>- [x] Define backup scope + recovery objectives for Postgres, object archive, and Valkey live-cache boundaries (`docs/how-to/dr-backup-restore-local.md`)<br>- [x] Implement reproducible local k3d backup/restore drill procedure with explicit pass/fail checks (`make dr-backup-local`, `make dr-restore-local`, `make dr-drill-local` + `docs/reference/commands.md`)<br>- [x] Execute restore drill end-to-end, capture command-output evidence, and update runbooks under `docs/`<br>- [x] Validation: run the documented backup + restore drill commands successfully<br>  - [x] Validation evidence (2026-03-05): `DR_MINIO_LOCAL_PORT=19021 make dr-backup-local DR_BACKUP_NAME=2026-03-05T2100Z` -> `backup report written to .../.tmp/dr-backups/2026-03-05T2100Z/report.env`<br>  - [x] Validation evidence (2026-03-05): `DR_MINIO_LOCAL_PORT=19021 make dr-restore-local DR_BACKUP_NAME=2026-03-05T2100Z` -> `restore completed`<br>  - [x] Validation evidence (2026-03-05): `DR_MINIO_LOCAL_PORT=19021 make dr-drill-local` -> `drill validation passed (db + archive object counts restored; actual >= backup baseline)` | M0+M1 |
| DONE | Alerts: lag/replay/auth/archive failures<br>- [x] Mark task `PROGRESS` and confirm dependency (`M0`) is complete<br>- [x] Define conservative baseline alert conditions and thresholds for ingest lag, replay failures, auth failures, and archive failures (windowed log-threshold + cooldown defaults)<br>- [x] Implement alert rules + local validation workflow and update command/runbook docs (`cmd/ecoflow-ingest-worker`, `cmd/ecoflow-gap-detector`, `cmd/ecoflow-gap-repair-worker`, `cmd/ecoflow-archive-worker`, `docs/reference/commands.md`)<br>- [x] Validation: execute failure simulations and capture alert evidence<br>  - [x] Validation evidence (2026-03-05): `go test -v ./internal/ingestworker -run TestReconnectRateTrackerAlertingAndCooldown -count=1` -> `PASS`<br>  - [x] Validation evidence (2026-03-05): `go test -v ./internal/gaprepair -run TestWorkerHandleDeliveryRunnerErrorNaks -count=1` -> `PASS`<br>  - [x] Validation evidence (2026-03-05): `go test -v ./internal/gaprepair -run TestFailureRateTrackerAlertingAndCooldown -count=1` -> `PASS`<br>  - [x] Validation evidence (2026-03-05): `go test -v ./internal/archiveworker -run TestProcessDeliveryStoreFailureNacks -count=1` -> `PASS`<br>  - [x] Validation evidence (2026-03-05): `go test -v ./internal/archiveworker -run TestFailureRateTrackerAlertingAndCooldown -count=1` -> `PASS` | M0 |

**Acceptance criteria**
- Releases gated by a small E2E suite
- Restore drills succeed
- Failure simulations show clean recovery

---

## M6 — Online ML recommendations (v1)
| Status | Task | Dependency |
|---|---|---|
| DONE | Define online inference contract (gRPC)<br>- [x] Review locked architecture, ADR-0001, ADR-0013, and existing telemetry/recommendation surfaces<br>- [x] Add protobuf contract for inference/insight reads + generated stubs<br>- [x] Wire authz-aware gRPC service scaffold into `cmd/ecoflow-grpc-api`<br>- [x] Add regression tests and validation evidence<br>Validation evidence (2026-03-08): `buf generate` succeeded; `go test ./...` succeeded; `make lint` succeeded. | M2 |
| DONE | Insights stream/projection<br>- [x] Review existing inference contract plus M2 projection/Valkey worker patterns<br>- [x] Add inference read-model store + derivation pipeline for first online insight slice (`BATTERY_EXPANSION` from provider-device capabilities + live ingest projection state)<br>- [x] Add runnable worker command and in-cluster services wiring (`cmd/ecoflow-inference-worker`, `pulse-services` worker deployment, local values, Docker image build)<br>- [x] Wire `InferenceService` to the persisted Valkey read model and add regression coverage (store, control-plane lookup, gRPC bootstrap fallback)<br>- [x] Validate locally and record evidence<br>Validation evidence (2026-03-08): `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml` succeeded; `go test ./...` succeeded; `make lint` succeeded. | M2 |
| DONE | Wire upsell modules to inference output<br>- [x] Review current battery upsell UI and merged inference read path<br>- [x] Add public device-insights REST adapter in the Node BFF (`/api/v1/devices/:deviceId/insights`)<br>- [x] Add universal app inference fetch/query wiring for device detail<br>- [x] Replace static battery upsell rendering with inference-backed content (with safe fallback behavior when inference is still pending/unavailable)<br>- [x] Validate end-to-end and record evidence<br>Validation evidence (2026-03-08): `npm run -w apps/pulse-platform test` succeeded; `npm run -w apps/pulse-platform typecheck` succeeded; `npm run -w apps/pulse-platform lint` succeeded; `npm run -w apps/universal test` succeeded; `npm run -w apps/universal typecheck` succeeded; `npm run -w apps/universal lint` succeeded; `go test ./...` succeeded; `make lint` succeeded. | M4 |

---

## Post-M6 — Product follow-ups
| Status | Task | Dependency |
|---|---|---|
| PROGRESS | Weather forecast service (`weatherd` over Open-Meteo)<br>- [x] Create Ralph-loop plan/task scaffolding under `.codex/` and start implementation on dedicated `codex/weatherd-profile-forecast` branch<br>- [x] Mark this architecture task `PROGRESS` before product-code changes<br>- [x] Add `pulse.weather.v1.WeatherService` protobuf contract and generated stubs<br>- [x] Implement Go Open-Meteo adapters, canonical cache keys, budget manager, hot cache, snapshot store, and gRPC handlers inside the existing `grpc-api` runtime<br>- [x] Add Postgres migrations for weather snapshots, forecast points, and recent-active refresh candidates<br>- [x] Add Node BFF weather routes backed by current-user weather profile coordinates with fixed panel defaults (`tilt=45`, `azimuth=0`)<br>- [x] Add universal profile weather widgets, attribution text, and deterministic mocked weather E2E spec coverage<br>- [x] Add shared compact header weather entry point and keep unavailable solar copy out of the UI chrome<br>- [ ] Validate targeted Go, Node, universal, and E2E flows; capture evidence before marking `DONE`<br>Validation evidence in progress (2026-03-18): `buf lint && buf generate` succeeded; `go test ./internal/weatherd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded; `go test ./internal/weatherd/... ./cmd/ecoflow-grpc-api/... -cover` succeeded; `npm run -w apps/pulse-platform test -- weather_routes.test.ts` succeeded; `npm run -w apps/pulse-platform typecheck` and `npm run -w apps/pulse-platform lint` succeeded; `npm run -w apps/universal test -- src/features/weather/api.test.ts src/features/weather/model.test.ts src/features/weather/hooks.test.ts` succeeded; `npm run -w apps/universal typecheck` and `npm run -w apps/universal lint` succeeded; `npm run -w apps/universal e2e:web -- profile-weather.spec.ts` succeeded after centralizing the Expo web Zustand compatibility shim; `make lint` succeeded.<br>Validation evidence in progress (2026-03-20): `npm run -w apps/universal test -- src/features/weather/model.test.ts src/features/weather/api.test.ts src/features/weather/hooks.test.ts` succeeded after adding the shared header weather pill, profile-weather-state helper, empty-solar suppression, on-demand forecast details, and kilometer visibility summaries on the large current/7-day weather widgets; `npm run -w apps/universal typecheck` succeeded after moving compact-widget wind data into the aligned top row and invalidating shared weather queries when saved profile weather inputs change; `npm run -w apps/universal e2e:web -- profile-weather.spec.ts` succeeded with the shared-header weather expectations.<br>Architecture note: [ADR-0023](./adr/ADR-0023-weather-forecast-service-open-meteo-snapshots-and-bias-correction.md) locks the internal weather service boundary, cache/snapshot model, upstream budget guardrails, and profile-driven public API path. The retired yesterday-verification RPC, route, storage, and local correction loop are documented in ADR-0023's 2026-05-24 amendment. | M1 auth/profile + M5 testing |
| PROGRESS | Forecast retention, canonical `4h` weather or solar refresh, and reusable background scheduling<br>- [x] Create Ralph-loop plan, task board, and memory scaffolding under `.codex/`<br>- [x] Mark this architecture task `PROGRESS` before product-code changes<br>- [ ] Add reusable scheduler or planner schema and worker runtime for future recurring workloads<br>- [ ] Compact weather persistence to bounded canonical-location snapshots and remove `weather_forecast_points` from the steady-state path<br>- [ ] Align weather refresh cadence and client freshness to `4h` without request-path re-fetch amplification<br>- [ ] Canonicalize solar persistence to scheduled site-level runs, then add bounded hot retention and prune paths<br>Current slice (2026-04-30): `cmd/ecoflow-scheduler` now seeds `solar.refresh_active_forecasts` with a default `24h` cadence. The job reconstructs recent active solar forecast inputs from persisted run metadata and calls `solarforecastd.GetSolarOutlook` so fresh training rows can be written without waiting for a user request; observed device PV evidence can raise stale allocated capacity after panel additions, and corroborated high PV-max evidence can fast-recover stale single-device envelopes while aggregate site series and isolated spikes stay bounded, so no operator-triggered retraining is required. Hot-data pruning remains on `solar.prune_hot_data`.<br>- [ ] Validate targeted Go, Node, universal, E2E, and Helm flows; capture data-savings and API-call evidence before marking `DONE` | weatherd + solar-forecastd + deploy |
 | PROGRESS | Solar generation forecast service (`solar-forecastd` with energy truth + weather features)<br>- [x] Mark this architecture task `PROGRESS` and define the deterministic-baseline slice for the current branch<br>- [x] Define the initial internal/public contract runway for actual-so-far, remaining-today forecast, 7-day totals, hourly peak timing, provenance, and confidence (`pulse.solarforecast.v1` proto scaffold)<br>- [x] Add a dedicated solar forecast domain boundary runway that consumes energy truth plus weather forecast snapshots without duplicating weather or energy ownership (`internal/solarforecastd` types + training-store seam)<br>- [x] Implement deterministic v1 using actual generation so far from the energy service and forecast irradiance for the remaining horizon in the universal profile path<br>- [x] Persist the first training-record runway for issue time, horizon, weather features used, actual later generation, and verification outcomes (migrations `000013_m1_solar_forecast_training` + `000014_m1_solar_forecast_weather_snapshot_nullable` + `000015_m1_solar_forecast_calibration_state` + `000016_m1_solar_forecast_served_variant_rollups` + `000017_m1_solar_forecast_shadow_baseline_hourly` + `000018_m1_solar_forecast_shadow_baseline_daily_rollups`)<br>- [x] Wire the internal `SolarForecastService` into the Go runtime and public BFF path<br>- [x] Replace the remaining app-only deterministic implementation with server-owned solar outlook responses<br>- [x] Add continuous verification metrics for hourly MAE/RMSE, daily kWh MAE, peak magnitude error, peak timing error, and horizon-bucket quality<br>Current slices landed: background verification loop, matured-hour backfill into `solar_forecast_hourly_training_records`, daily rollup recomputation, low-cardinality coverage/absolute-error/peak-error metrics, exact hourly RMSE telemetry derived from verified squared-error totals by horizon, served-variant quality splits for calibrated vs baseline panels, shadow-baseline persistence so calibrated runs can be compared against the uncali­brated baseline on the exact same issued forecast, and daily rollup shadow-baseline day/peak fields so Grafana can compare served vs baseline quality beyond hourly RMSE.<br>- [x] Add site-specific calibration/bias learning before ML promotion, while keeping the deterministic baseline as the benchmark/fallback<br>Current slices landed: persisted site-key/hour-of-day/horizon calibration state with clamped EWMA ratio learning from verified solar rows, calibrated serving on the forecast path, public provenance fields that distinguish the served variant from the baseline model without changing the benchmark version key, replay-style regression coverage proving a later site-calibrated run can beat the same-run shadow baseline on daily, peak-power, and peak-timing quality once enough verified history exists, a tighter same-day forecast heuristic that preserves realistic peak potential while derating cloudy/rainy hours and clamping remaining-today estimates when observed generation is lagging the day profile, and a deduped recent-site calibration fallback that starts tightening tomorrow/7-day forecasts after one fully verified local day even before the slower hour-bucket EWMA has matured.<br>- [x] Add BFF + universal app solar outlook surfaces that explicitly separate actual generation from forecasted remainder<br>- [x] Add device-scoped solar outlook serving and persistence so `scope=device` uses one device's history/training truth instead of reusing the site aggregate fit<br>- [x] Add device-aware calibration/serving-state wiring so DPU-class devices can learn precise per-device envelopes without regressing the overall site forecast<br>- [x] Extend gRPC/BFF/universal solar outlook consumers so device detail, filtered `/energy`, and profile weather widgets can request either site or selected-device forecasts with stable caching<br>- [x] Add a profile-page solar device picker next to the weather widgets, mirroring `/energy` filter behavior while defaulting the unfiltered profile view to the whole site<br>- [x] Replay recent archived DPU/D2M runs against the new device-scoped path, capture fit evidence, and update docs before marking this task `DONE`<br>- [ ] Add observability and SLO dashboards for forecast availability, freshness, active sites, model mix, and forecast-vs-actual quality<br>Latest slices landed: request/training persistence counters, confidence mix, verifier coverage/error metrics, served model-mix telemetry, a rolling 24h active-site gauge, serve/issue/verification freshness gauges, variant-aware RMSE/quality panels, served-vs-shadow-baseline RMSE comparison, served-vs-shadow-baseline daily/peak quality comparison, and a Grafana solar forecast service dashboard. Richer replay-backed validation and incident-grade SLO alert wiring remain follow-up work.<br>- [ ] Validate with realistic historical replays and live local product flows before marking `DONE`<br>Validation evidence in progress (2026-03-19): `buf generate` succeeded after adding solar provenance fields for served variant and calibration metadata to `pulse.solarforecast.v1`; `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` and `go test ./internal/integrationtest -count=1` succeeded after extending migration CI coverage through `000017_m1_solar_forecast_shadow_baseline_hourly`; `helm lint deploy/charts/pulse-platform -f deploy/env/local/values.platform.yaml` succeeded after adding the solar Grafana availability/training/verification/model-mix/active-site/freshness/RMSE/variant-split/shadow-baseline panels; `npm run -w apps/pulse-platform typecheck` and `npm run -w apps/pulse-platform test -- weather_routes.test.ts` succeeded with the `/api/v1/solar/outlook` route returning the new calibration provenance fields; `npm run -w apps/universal typecheck` and `npm run -w apps/universal test -- src/features/weather/api.test.ts src/features/weather/hooks.test.ts src/features/weather/model.test.ts` succeeded after wiring the solar provenance parser and calibrated-forecast UI copy; `npm run -w apps/universal e2e:web -- profile-weather.spec.ts` succeeded with the updated mock solar route; replay-style regression coverage succeeded via `TestReplayValidationCalibratedRunBeatsShadowBaseline`, `TestGetSolarOutlookAppliesCalibrationRatio`, and `TestVerifyIssuedForecastsBackfillsActualsAndRollups`; local live validation succeeded after `make db-migrate-up-local`, `make dev-grafana`, and `DEV_DEPLOY_HELM=never make dev-deploy`: a real Keycloak sign-in to `https://localhost/profile` loaded the weather forecast, yesterday verification, and solar outlook BFF routes with `200` responses; all `grpc-api` pods reported successful `Get7DayForecast`, `GetYesterdayVerification`, and `GetSolarOutlook` unary calls with `14ms`, `9ms`, and `54ms` durations; Prometheus instant queries showed one successful solar forecast request for `scope=all`, one `baseline` model-mix event, one active solar site, and successful weather requests served from fresh cache and stored verification; `make lint` succeeded after wiring solar metrics, best-effort training persistence, background verification, daily rollups, persisted calibration state, served-variant rollup persistence, shadow-baseline hourly persistence, service regression coverage for device/all-scope training writes, verification backfill, calibration application, the new served-vs-baseline provenance mapping, rolling active-site/freshness telemetry, and RMSE observability derived from verified hourly squared-error totals.<br>Validation evidence in progress (2026-03-20): `go test ./internal/solarforecastd/... -count=1` succeeded after tightening the deterministic baseline with weather attenuation, system-efficiency derates, corrected observed-peak math, a same-day remaining-horizon clamp driven by actual generation so far, and a deduped recent-site calibration fallback keyed by latest verified `target_time` rows; `go test ./cmd/ecoflow-grpc-api/... ./internal/solarforecastd/... -count=1` succeeded with the updated `GetSolarOutlook` path and regression coverage for bad-weather derates, lagging-day scaling, 24-hour recent-site learning activation, and duplicate-refresh deduping by latest issue.<br>Validation evidence in progress (2026-03-21): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded after removing the duplicate weather attenuation from irradiance-based watts, teaching recent-site calibration to accept DST-shortened verified days, and keeping the compact solar summary numerically honest by separating actual-so-far from remaining/total forecast wording; `npm run -w apps/universal test -- src/features/weather/model.test.ts src/features/weather/api.test.ts src/features/profile/hooks.test.ts src/features/weather/hooks.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `npm run -w apps/universal lint` completed with only the long-standing unrelated React hook warnings in the energy tab and device card; `make lint` succeeded.<br>Validation evidence in progress (2026-03-21, CPU/latency follow-up): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded after moving best-effort training persistence off the unary request path, raising solar outlook UI freshness from `30s` to `5m`, and activating the profile weather section lazily once it is near the viewport instead of eagerly fetching below-the-fold content; `npm run -w apps/universal test -- src/features/weather/hooks.test.ts src/features/profile/hooks.test.ts src/features/weather/model.test.ts src/features/weather/api.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `make lint` succeeded.<br>Validation evidence in progress (2026-03-22): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded after shifting site-potential inference to a rolling observed `P95` envelope, keeping calibration re-capped to the learned site peak, converting secondary weather suppression to bounded caps instead of stacked multipliers, and computing the same-day remaining-horizon scale from the irradiance/temperature baseline instead of already-suppressed past hours; `SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentSolarRunsAgainstCurrentBranch -count=1 -v` replayed recent archived runs and showed the representative March 21 bad-weather overprediction tighten from `4.70 kWh` issued / `3.10 kWh` earlier replay to `2.60 kWh` against `1.95 kWh` actual, while March 20 held at `1.60 kWh` replay against `1.16 kWh` actual and March 19 remained aligned at `5.70 kWh` replay against `5.68 kWh` actual.<br>Validation evidence in progress (2026-03-22, review-follow-up): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded after restoring same-day remaining-horizon scaling to the modeled hourly forecast path, letting rolling `P95` capacity recover part of the gap toward observed site peak on strong-irradiance rebound days, using saturated historical hours as evidence of understated site potential, and adding a same-day SOC saturation-band curtailment cap for repeated `98–100%` battery hysteresis hours; `SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentSolarRunsAgainstCurrentBranch -count=1 -v` replayed recent archived runs and further tightened the representative March 21 bad-weather case to `2.40 kWh` replay against `1.95 kWh` actual while preserving March 19 alignment at `5.70 kWh` replay against `5.68 kWh` actual; March 20 remained conservative-work-in-progress at `1.70 kWh` replay against `1.16 kWh` actual, which keeps the branch bounded to the review comments plus SOC-curtailment slice rather than another broader weather-model retune.<br>Validation evidence in progress (2026-03-22, charge-aware curtailment + provenance): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded after making same-day battery saturation curtailment depend on low `BatteryChargeEnergyWh` instead of SOC alone, treating repeated charge-constrained saturated days as bounded potential-uplift evidence over the rolling observed `P95` envelope, and adding display-safe same-day curtailment provenance to the solar contract/UI; `npm run -w apps/pulse-platform typecheck` and `npm run -w apps/pulse-platform test -- test/solarForecastClient.test.ts test/weather_routes.test.ts` succeeded after extending the BFF solar outlook mapping for same-day curtailment provenance; `npm run -w apps/universal typecheck` and `npm run -w apps/universal test -- src/features/weather/api.test.ts src/features/weather/model.test.ts` succeeded after replacing the verbose dual-line solar explanation with one compact provenance line in the large current weather widget; `make lint` succeeded; `SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentSolarRunsAgainstCurrentBranch -count=1 -v` replayed recent archived runs and held March 22 at the old issued `1.50 kWh` while still tightening March 21 from `4.70 kWh` issued to `2.90 kWh` replay against `1.95 kWh` actual, with March 20 remaining the next weather-day retune candidate at `1.60 kWh` replay against `1.16 kWh` actual.<br>Validation evidence in progress (2026-03-25, future-day weather-regime suppression + replay hygiene): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... -count=1` succeeded after adding a true-future-day weather-regime suppression factor inside the modeled forecast path so low-visibility / fog / rain / wet-low-visibility hours can reduce both future-day energy and post-calibration peak caps without perturbing same-day remaining-hour clamping; `SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentSolarRunsAgainstCurrentBranch -count=1 -v` now computes completed-day actuals from direct full-day rollup totals, skips current local days and near-zero broken-device days from replay selection, and showed March 23 tighten from old issued `2.50 kWh` to `2.40 kWh` replay against `2.39 kWh` actual while March 24 remained the next strong future-day retune candidate at `11.40 kWh` replay against `8.99 kWh` actual and March 22 remained conservative-work-in-progress at `1.70 kWh` replay against `1.41 kWh` actual.<br>Validation evidence in progress (2026-03-26, serving-state refactor + late-day clamp): `go test ./internal/solarforecastd/... ./cmd/ecoflow-grpc-api/... ./internal/integrationtest -count=1` succeeded after moving recent-site calibration and 7-day potential-envelope reconstruction off the `GetSolarOutlook` request path into background serving-state refreshes, switching the live path to tiny serving-state reads plus today-only telemetry, adding migration `000019_m1_solar_forecast_serving_state_and_indexes` for the new serving-state table and hot lookup indexes, adding migration `000020_m1_solar_forecast_recent_calibration_index` for the recent-calibration partial index, and adding a late-day trailing-hours clamp so same-day remaining forecasts respect the last completed production shoulder after 2 PM local time; `make lint` succeeded; `make db-migrate-up-local` and `make dev-deploy` restored the live profile flow with `/api/v1/solar/outlook` returning `200`, the profile weather card repopulating solar estimates for today and the full 7-day forecast, and fresh `GetSolarOutlook` unary calls completing in `7-30ms` instead of timing out; replay remained stable with March 24 slightly tightened from `11.50 kWh` to `11.40 kWh` against `8.99 kWh` actual while March 23 moved from `2.20 kWh` to `2.10 kWh` against `2.39 kWh` actual, so the main win in this slice is high-throughput serving-path safety rather than a large fit jump.<br>Validation evidence in progress (2026-03-28, per-device product slice): `npm run -w apps/pulse-platform test -- weather_routes.test.ts solarForecastClient.test.ts` succeeded after adding `scope=device` request parsing to `/api/v1/solar/outlook`; `npm run -w apps/pulse-platform typecheck` succeeded; `npm run -w apps/universal test -- src/features/weather/api.test.ts src/features/weather/hooks.test.ts` succeeded after partitioning solar query keys by `scope/deviceId`; `npm run -w apps/universal typecheck` succeeded after wiring the profile solar-scope picker and device-detail forecast card; `GOCACHE=/tmp/ecoflow-go-cache go test ./internal/solarforecastd -count=1` succeeded; `GOCACHE=/tmp/ecoflow-go-cache SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentPerDeviceSolarRunsAgainstCurrentBranch -count=1 -v` succeeded and showed device-scoped replay remaining materially high on recent days (`2026-03-27`: `3.82 -> 9.80 kWh` and `6.39 -> 11.70 kWh`; `2026-03-26`: `0.69 -> 3.00 kWh` and `1.78 -> 3.70 kWh`), confirming the next follow-up is deeper per-device calibration/serving-state tuning rather than more UI wiring.<br>Validation evidence in progress (2026-03-28, per-device site-context allocation slice): `GOCACHE=/tmp/ecoflow-go-cache go test ./cmd/ecoflow-grpc-api ./internal/solarforecastd -count=1` succeeded after teaching device-scoped requests to carry full-site context, anchoring `scope=device` forecasts to the calibrated site outlook, allocating device totals and peaks by recent measured device share, and persisting `site_resolved_device_ids` so replay can reconstruct the same path later; `npm run -w apps/pulse-platform test -- weather_routes.test.ts solarForecastClient.test.ts`, `npm run -w apps/pulse-platform typecheck`, `npm run -w apps/universal test -- src/features/weather/api.test.ts src/features/weather/hooks.test.ts`, and `npm run -w apps/universal typecheck` all succeeded against the same branch; a fresh live per-device replay pass remains blocked until the local replay Postgres endpoint at `127.0.0.1:15432` is reachable again because `SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentPerDeviceSolarRunsAgainstCurrentBranch -count=1 -v` currently fails with `dial tcp 127.0.0.1:15432: connect: connection refused`.<br>Architecture note: [ADR-0024](./adr/ADR-0024-solar-generation-forecast-service-energy-truth-weather-features-and-site-learning.md) proposes the dedicated solar forecast domain, energy-truth-first data model, and staged path from deterministic baseline to site-learned ML forecasting. | ADR-0018 + ADR-0019 + ADR-0023 + M5 |
|  | Validation evidence in progress (2026-03-28, site-allocated device follow-up): `GOCACHE=/tmp/ecoflow-go-cache go test ./internal/solarforecastd -count=1` and `go test ./cmd/ecoflow-grpc-api -count=1` succeeded after carrying full-site device context through the gRPC boundary and anchoring `scope=device` forecasts to a site-calibrated, recent-device-share allocation path; `GOCACHE=/tmp/ecoflow-go-cache SOLAR_REPLAY_DSN=postgres://pulse:pulse-local-dev-password@127.0.0.1:15432/pulse?sslmode=disable go test ./internal/solarforecastd -run TestReplayRecentPerDeviceSolarRunsAgainstCurrentBranch -count=1 -v` succeeded after replay began preserving site context for device slices and tightened recent per-device fit materially (`2026-03-27`: `3.82 -> 4.30 kWh` and `6.39 -> 9.10 kWh`; `2026-03-26`: `0.69 -> 1.20 kWh` and `1.78 -> 2.90 kWh`), leaving cloudy-day overprediction as the next tuning target rather than the earlier whole-site duplication bug. |  |
|  | Validation evidence in progress (2026-03-29, device-scope widget follow-up): `npm run -w apps/universal test -- src/features/weather/api.test.ts src/features/weather/hooks.test.ts src/features/weather/model.test.ts` and `npm run -w apps/universal typecheck` succeeded after aligning the universal solar parser with new `*_device_share` capacity methods, threading explicit forecast scope into shared header weather widgets, and surfacing solar-outlook loading/errors instead of silently falling back to site totals; `make dev-web-deploy` then completed successfully on `k3d-pulse-local`, and the local public app plus realtime gateway returned to `2/2` ready while serving the refreshed frontend bundle. |  |
| DONE | Client-observed REST SLOs + Grafana dashboards (ADR-0021)<br>- [x] Review Google SRE SLI guidance and current Pulse observability/auth-profile dashboard wiring<br>- [x] Capture the client-observed REST SLI decision in ADR-0021 and codify the repo rule in `AGENTS.md`<br>- [x] Instrument universal-app REST requests with canonical client-observed latency / availability / error events<br>- [x] Add public-edge ingestion path + Prometheus metrics for client-observed REST outcomes<br>- [x] Build Grafana client-metrics SLO dashboard panels for availability, latency, and errors, then add supporting context panels per best practices<br>- [x] Validate locally and capture evidence before marking DONE<br>Validation evidence (2026-03-15): `npm run -w apps/universal test -- src/shared/api/restClient.test.ts src/features/auth/sessionRecoveryCoordinator.test.ts src/features/auth/loginNotice.test.ts src/features/auth/useRequireAuth.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `npm run -w apps/pulse-platform test -- me_routes.test.ts` succeeded; `npm run -w apps/pulse-platform typecheck` succeeded; `make lint` succeeded; the user confirmed `Pulse Client REST SLOs` was flowing cleanly in local Grafana after deploy. | ADR-0021 + M1 auth/public edge + observability-lite |
| DONE | Client-observed WebSocket SLOs + Grafana dashboards (ADR-0022)<br>- [x] Review current universal telemetry-engine lifecycle, realtime gateway behavior, and client-visible reconnect/staleness states<br>- [x] Capture the client-observed WebSocket SLI decision in ADR-0022 and codify repo rules alongside the REST SLI policy<br>- [x] Instrument universal-app websocket lifecycle with canonical client-observed connect/reconnect latency, disconnect reason, auth-required/auth-failed, and freshness events<br>- [x] Add public-edge ingestion path + Prometheus metrics for client-observed websocket outcomes<br>- [x] Build Grafana client websocket SLO dashboard panels for availability, latency, errors, and freshness context<br>- [x] Validate locally and capture evidence before marking DONE<br>Validation evidence (2026-03-15): `npm run -w apps/universal test -- src/features/telemetry/engine/TelemetryEngine.test.ts src/shared/api/restClient.test.ts src/features/auth/sessionRecoveryCoordinator.test.ts src/features/auth/loginNotice.test.ts src/features/auth/useRequireAuth.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `npm run -w apps/pulse-platform test -- me_routes.test.ts` succeeded; `npm run -w apps/pulse-platform typecheck` succeeded; `make lint` succeeded; `make platform-up`, `make platform-wait`, and `make dev-web-deploy` were run locally; the user confirmed `Pulse Client WebSocket SLOs` looked healthy in Grafana after live refresh/reconnect traffic. | ADR-0008 + M1 realtime/public edge + observability-lite |
| DONE | ADR-0018 explicit energy rollup buckets: schema + writer + query-path persisted-first<br>- [x] Kick off on fresh `codex/energy-rollup-buckets` branch after Energy dashboard merge and capture scope from ADR-0018 plus existing M3 rollup/query docs<br>- [x] Add backward-compatible rollup schema migrations for explicit energy buckets on minute/hour/day tables<br>- [x] Update rollup extraction/materialization so minute buckets persist explicit energy fields from normalized telemetry inputs<br>- [x] Update hour/day aggregation to sum persisted explicit energy buckets instead of re-deriving from average power<br>- [x] Update Go telemetry query / Energy dashboard read paths to prefer persisted explicit buckets with temporary fallback only where coverage is incomplete<br>- [x] Add regression coverage for schema/read/write paths, including at least one DPU-class and one D2M-class fixture path<br>- [x] Record validation evidence and backfill/rebuild follow-up notes before marking DONE<br>Validation evidence (2026-03-11): `go test ./cmd/ecoflow-grpc-api ./internal/rollupworker ./internal/telemetryquery ./internal/integrationtest -count=1` succeeded; `make lint` succeeded.<br>Follow-up: the historical fallback has now been removed; legacy windows must be repaired through archive-to-rollup rebuilds if persisted explicit energy buckets are missing. | ADR-0018 + M3 |
| DONE | ADR-0018 explicit energy bucket backfill/rebuild coverage<br>- [x] Add archive-to-rollup rebuild support for the new explicit energy columns<br>- [x] Define/record bounded local and dev backfill procedure for existing minute/hour/day windows<br>- [x] Add coverage/telemetry so read paths can report persisted-vs-fallback usage during migration<br>- [x] Remove fallback after backfill coverage is complete and validated<br>Validation evidence (2026-03-11): `go test ./internal/integrationtest -run TestMigrationsCycleAndE2E -count=1` succeeded after adding bounded retry for transient Timescale retention-policy deadlocks in migration CI; `go test ./cmd/ecoflow-grpc-api ./internal/rollupworker ./internal/telemetryquery ./internal/integrationtest ./internal/rolluprebuild ./cmd/ecoflow-rollup-rebuild -count=1` succeeded; `make lint` succeeded.<br>Validation evidence (2026-03-14): `go test ./internal/archiveaudit ./internal/envelopededup ./internal/dbmigrate ./internal/replaycli ./internal/rolluprebuild ./internal/rollupworker ./internal/telemetryquery ./cmd/ecoflow-archive-audit ./cmd/ecoflow-archive-reconcile ./cmd/ecoflow-rollup-rebuild ./cmd/ecoflow-db-migrate-job -count=1` succeeded; `helm lint deploy/charts/pulse-platform -f deploy/env/local/values.platform.yaml` succeeded; `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml` succeeded after adding direct MinIO archive audit/reconcile tooling, envelope-idempotent rollup writes/rebuilds, durable local raw-archive MinIO defaults, and idempotent local migration adoption for pre-ledger clusters.<br>Validation evidence (2026-03-14): `go test ./internal/telemetryquery ./cmd/ecoflow-grpc-api -count=1` succeeded and `make lint` succeeded after removing query-time energy fallback so history reads now leave missing explicit Wh buckets unset instead of deriving them from average power.<br>Notes: archive/raw-log rebuild repopulates explicit energy bucket columns for historical windows, and the read path now treats those persisted columns as authoritative rather than synthesizing replacements when legacy buckets are incomplete. | ADR-0018 + M2 + M3 |
| DONE | ADR-0019 split energy RPCs into dedicated `EnergyService`<br>- [x] Capture architecture change in a new ADR that supersedes ADR-0018's service-boundary non-goal<br>- [x] Split protobuf service definitions so energy/history RPCs move out of `TelemetryService`<br>- [x] Move Go server implementation to a dedicated `EnergyService` while preserving ADR-0013 server tuning/interceptor baseline<br>- [x] Update Node BFF grpc client and REST handlers to call `EnergyService`<br>- [x] Deploy telemetry and energy gRPC workloads independently in local/dev Helm values while keeping the shared `ecoflow-grpc-api` binary and ADR-0013 baseline intact<br>- [x] Validate proto generation, Go tests, Node tests, chart renders, and local deploy end to end<br>Validation evidence (2026-03-11): `buf generate` succeeded; `go test ./cmd/ecoflow-grpc-api -count=1` succeeded; `npm run -w apps/pulse-platform typecheck` succeeded; `npm run -w apps/pulse-platform test` succeeded; `helm lint deploy/charts/pulse-services -f deploy/env/local/values.services.yaml` succeeded; `helm lint deploy/charts/pulse-platform -f deploy/env/local/values.platform.yaml` succeeded; `make lint` succeeded; `make dev-deploy` succeeded with `pulse-services-go-energy-api` reaching `3/3` Ready and live requests logging `/pulse.telemetry.v1.EnergyService/QueryRollupRange` and `/pulse.telemetry.v1.EnergyService/CompareRollupRange` on the new deployment. | ADR-0019 + ADR-0013 |
| DONE | EnergyService latency reduction: SQL-side fleet aggregation + concurrent compare/dashboard reads<br>- [x] Add multi-device rollup query support so fleet energy windows aggregate in Postgres instead of serial Go-side merges<br>- [x] Run current/previous compare-window reads concurrently for `CompareRollupRange`<br>- [x] Parallelize dashboard current/previous energy + power reads so independent subqueries do not serialize on the request path<br>- [x] Make Energy dashboard tests deterministic with an injectable service clock instead of relying on real wall-clock `today` windows<br>- [x] Shift `today`/`yesterday` power off minute-level hot-path reads into server-side `5-minute` rollups while keeping week-scale presets visually consistent with shared hourly density<br>- [x] Make `last7d` a closed local-calendar window of seven completed days so it excludes today and aligns point-count semantics with other closed historical presets<br>- [x] Lazy-load PV port history behind a dedicated `EnergyService` RPC/REST path so archive-backed history does not block the main dashboard payload<br>- [x] Measure before/after local latency on the live `EnergyService` deployment and capture the result<br>- [x] Persist PV port history into database-backed rollups/read models so `GetEnergyPvPortHistory` no longer depends on archive-object scans on the steady-state request path<br>- [x] Apply the PV-port rollup migration locally, deploy the updated energy/rollup services, and capture new live `GetEnergyPvPortHistory` timings from the database-backed path<br>Validation evidence (2026-03-12): `go test ./internal/telemetryquery ./cmd/ecoflow-grpc-api ./internal/rollupworker ./internal/rolluprebuild -count=1` succeeded; `npm run -w apps/pulse-platform typecheck` and `npm run -w apps/pulse-platform test -- history_routes.test.ts` succeeded; `npm run -w apps/universal typecheck` and `npm run -w apps/universal e2e:web -- energy.spec.ts` succeeded; `make lint` succeeded.<br>Validation evidence (2026-03-14): `go test ./internal/energydashboard ./cmd/ecoflow-grpc-api -count=1` succeeded; `make lint` succeeded; live `pulse-services-go-energy-api` logs on `k3d-pulse-local` showed `GetEnergyDashboard` at `21-38ms` and `GetEnergyPvPortHistory` cold misses at `96-162ms`/`122ms`, with repeated warm hits at `3ms` after adding bounded-parallel archive decoding plus a short TTL request cache.<br>Validation evidence (2026-03-14, medium-term implementation): `go test ./internal/rollupworker ./internal/rolluprebuild ./internal/telemetryquery ./internal/integrationtest ./cmd/ecoflow-grpc-api -count=1` succeeded; `make lint` succeeded after adding persisted PV-port minute/hour/day rollup tables, rollup/rebuild writers, DB-backed `QueryPVPortHistory`, and EnergyService tests preferring the persisted path.<br>Validation evidence (2026-03-14, live persisted path): `make db-migrate-up-local` succeeded with migration `000011_m3_pv_port_history_rollups` applied; the new `telemetry_rollup_pv_port_minute/hour/day` tables were populated in local Postgres (`20/8/4` rows at verification time); the user confirmed `GetEnergyPvPortHistory` was visible and healthy in the local gRPC latency dashboard after refreshes on `/energy`.<br>Measured local result before the short-term optimization pass: local `GetEnergyPvPortHistory` calls were landing around `104ms`, `124ms`, `143ms`, `173ms`, `198ms`, `245ms`, `267ms`, `284ms`, `294ms`, and `598ms` from the energy-api logs under the same localhost usage pattern.<br>Validation evidence (2026-03-21): targeted PV-port extraction regressions now reject `outWatts`/`inWatts` as per-port watt fallbacks for multi-port Delta-class devices, so live device details and persisted PV-port history no longer attribute total-device MPPT watts to `pv-1`/`pv-low`; the Energy-screen PV operating envelope now preserves displayed observed/history watts above each port’s configured ceiling while keeping configured watts as envelope metadata, so provider samples above nominal max remain visible. `go test ./internal/rollupworker ./internal/energydashboard -count=1`, `npm run -w apps/pulse-platform test -- provider_device_mapper.test.ts`, and `npm run -w apps/universal test -- src/features/energy/model.test.ts` succeeded. | ADR-0019 + M3 |
|  | Correction (2026-04-30): the Energy-screen PV operating envelope no longer caps displayed observed or historical watts by configured port power. Configured watts remain envelope metadata and the utilization denominator; malformed-telemetry guards stay in mapper/rollup paths. |  |
| DONE | Energy dashboard comparison status widget<br>- [x] Define product semantics for what counts as a meaningful comparison outcome across current vs previous windows<br>- [x] Decide whether the first version is deterministic/rule-based, ML-assisted, or hybrid, and document the upgrade path if ML is deferred<br>- [x] Add backend comparison-analysis contract and server-side derivation over the existing energy/power summary data<br>- [x] Render a stable widget on `/energy` with truthful explanations, explicit confidence/limitations, hourly caching, and compare-always behavior independent from the chart toggle<br>- [x] Add regression coverage for comparison classification, copy rendering, and edge cases such as flat usage, missing baseline, and mixed signals<br>- [x] Record validation evidence and follow-up scope before marking DONE<br>Validation evidence (2026-03-12): `buf generate` succeeded; `go test ./cmd/ecoflow-grpc-api ./internal/inference -count=1` succeeded; `npm run -w apps/pulse-platform test -- history_routes.test.ts` succeeded; `npm run -w apps/universal e2e:web -- energy.spec.ts` succeeded; `make lint` succeeded; `make dev-deploy` succeeded; live localhost checks returned `HTTP 200` for both fleet `GET /api/v1/energy/comparison-insight?scope=all&preset=last7d...` and single-device `GET /api/v1/energy/comparison-insight?scope=device&deviceId=<uuid>&preset=last7d...`, each with a ready comparison verdict and 4 dynamic evidence-backed cards.<br>Follow-up scope: upgrade the current weighted heuristic to a dedicated M6 model path and add explicit telemetry for inference freshness, verdict drift, and per-card user interaction quality. | M3 + M6 |
| DONE | Storm Guard universal fleet banner<br>- [x] Confirm product semantics from archived DPU evidence: active only when the device reports both `stormPatternEnable` and `stormPatternOpenFlag`; do not infer from weak PV alone<br>- [x] Verify the Storm Guard fields flow through the live normalized/projection path and extend mapping if they do not<br>- [x] Expose device-reported Storm Guard state and end time on the existing device/snapshot contract for visible devices<br>- [x] Derive a fleet-level “any visible supported EcoFlow device active” state and show a universal toast/banner across device views when active<br>- [x] Add regression coverage for mapper, API contract, and universal UI rendering across fleet and single-device contexts<br>- [x] Record validation evidence before marking DONE<br>Validation evidence (2026-03-12): external EcoFlow device docs were checked for Storm Guard field families (`stormPatternEnable` / `stormPatternOpenFlag` / `stormPatternEndTime` and `stormIsEnable` / `inStormMode` / `stormEndTimestamp`) and the mapper now scans normalized metadata groups generically instead of hardcoding one DPU-only path; `npm run -w apps/pulse-platform test -- provider_device_mapper.test.ts device_client.test.ts` succeeded; `npm run -w apps/universal typecheck` succeeded; `npm run -w apps/universal e2e:web -- devices.spec.ts energy.spec.ts` succeeded; `make dev-deploy` succeeded; live `GET /api/devices` on localhost returned device payloads carrying `details.stormGuardActive` on visible devices, confirming the end-to-end contract is wired for the current fleet and future supported EcoFlow models that expose either documented field family. | M2 + M4 |
| DONE | Energy dashboard (`/energy`)<br>- [x] Draft loop plan under `.codex/plans/energy-dashboard-ralph-loop.md` and bootstrap `.codex/tasks` + `.codex/memories` scaffolding<br>- [x] Mark tracked task `PROGRESS` and add checkbox breakdown before product code starts<br>- [x] Backend gap audit: current rollup/query path is single-device and only persists aggregate power/SOC/temp plus `solar_generated_wh`; explicit energy buckets and historical per-PV-port observations require backend expansion before BFF/UI work<br>- [x] Go energy dashboard contract + server-side aggregation/query layer with authz-aware all-device scope and calendar-window comparison<br>- [x] Node BFF `GET /api/v1/energy/dashboard` route + validation + tests<br>- [x] Universal `/energy` route, URL-state sync, summary cards, energy/power charts, responsive PV envelope, battery/insights/price sections, and stable loading states<br>- [x] QA coverage: DST-sensitive window math, authz-aware query path, BFF contract, frontend rendering, and Playwright web flow<br>- [x] Product-review alignment fixes from live local walkthroughs, including stale-archive graceful degradation, compare-off row hiding, solar-generation-based estimated value, deep-linked Energy entry points, and accepted localhost UX review<br>- [x] Final validation evidence, docs sync, and local k3d deploy review captured<br>- [x] Long-term rollup direction captured in ADR-0018: persist explicit energy buckets instead of keeping query-time derivation as the steady-state architecture<br>Validation evidence (2026-03-11): `make lint` succeeded; `go test ./internal/energydashboard ./internal/telemetryquery ./internal/grpcmw ./cmd/ecoflow-grpc-api -count=1` succeeded; `npm run -w apps/universal e2e:web -- devices.spec.ts energy.spec.ts` succeeded; `make dev-deploy` succeeded and localhost review accepted the deployed Energy dashboard. | M3 + M4 + M5 + ADR-0018 |

---

# 9) Lock statement

**This plan is locked** for development start:
- build the platform baseline (M0)
- then identity/control plane (M1)
- then telemetry+replay (M2)
- then rollups+history (M3)
- then realtime UX (M4)
- then tests/ops hardening (M5)

Any changes should be logged as a deliberate decision (with rationale) and rolled into this document.
